{
  "name": "rentos-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end tests for the RentOS platform (client + server) using Playwright.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:local": "E2E_API_URL=$(node detect-api-port.js) DEV_CLIENT_PORT=$(node detect-port.js) PLAYWRIGHT_REUSE_SERVER=1 playwright test",
    "install-browsers": "playwright install --with-deps chromium",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^24.12.0"
  }
}
