# Ghana and app-store compliance delivery

Audit started 12 September 2026 by Codex; continued by Claude from 25 September 2026. Status: engineering substantially complete; external obligations open (see table).

This ledger tracks engineering evidence and external obligations separately. It does not certify that every Ghana law applies, or that RentOS holds any registration, licence or store approval. Prior audit completion claims must be reconciled against current code and runtime.

| ID | Scope | Status | Evidence / remaining work |
|---|---|---|---|
| C01 | Rental agreement controls | ENGINEERING DONE; legal review pending | Codex's agreement checks, plus (25 Sep): only tenant-signed leases establish a tenancy (contact details, credit, disputes, reviews, maintenance, rent payments, deductions); Act 772 signature evidence (identity, time, IP, device, terms hash, consent) on agreements, renewals and financing contracts; renewals need the tenant's signature before terms change; s.25 advance caps on agreements, listings and rent advances. Open: lawyer review of generated agreements, Terms and legal articles. |
| C02 | Privacy and erasure | ENGINEERING DONE; operator evidence pending | Export/erasure (Codex), plus: versioned Terms/Privacy/18+ consent with evidence; privacy notice rebuilt from the processor inventory; retention periods in `config/retention.ts` with TTLs; tombstone minimised; Ghana Card numbers AES-256-GCM encrypted, last-4 for landlords and admins (audited reveal), none for government; religion/ethnicity removed; shared passports minimal. Open: DPC registration, `LEGAL_ENTITY` details, backup/provider erasure, run cleanup scripts in production. |
| C03 | Consent and AI processing | ENGINEERING DONE; legal review pending | Per-request AI sharing consent (Codex); notification preferences enforced per channel and category with unsubscribe links; promotions in-app only (no marketing consent collected); automated loan decisions only pre-qualify with reasons and a human-review route; credit scoring gated. |
| C04 | UGC safety | ENGINEERING DONE; operations pending | Blocking and chat reports (Codex); reports on listings, reviews, businesses, workers (web and mobile); text filter on chat and reviews with auto-reports; moderation and off-plan review queues; Terms conduct clause with 24-hour action commitment. Open: staffed moderation, Twi word-list review, native-device walkthrough. |
| C05 | Store payments | ENGINEERING DONE; sandbox evidence pending | IAP-only digital plans with restore/manage and server verification (Codex); rent and real-world services external; regulated money features gated. Open: StoreKit/Play sandbox evidence with production products. |
| C06 | Native release | ENGINEERING DONE; submission pending | Brand icons/splash (mobile splash matches web), permissions blocked or requested in context, export compliance, remote build numbers, MFA sign-in, deep links mapped, dead controls removed, AI disclaimer, public /support page, `docs/compliance/store-submission.md`. Open: signed builds and privacy report, device runs, store forms, review outcomes. |
| C07 | Ghana regulated activities | GATED IN CODE; licences EXTERNAL | Server-side gate for rent collection, wallet, lending, financing, investments, insurance, payroll and credit reporting: off in production unless licensed basis recorded (`docs/compliance/regulated-features.md`); real firm names removed; lender/insurer/financier licence verification; REAC licence recording for agencies. Open: determine operator/partner roles and obtain licences before enabling any feature. |
| C08 | Ghana tax, commerce and contracts | PARTIAL | Terms cover refunds, fixed-term mobile-money plans vs store auto-renew, taxes at checkout, electronic signatures (Act 772) without guaranteeing enforceability. Open: verify GRA rent tax/withholding/VAT duties, show applicable taxes at checkout, seller identity, ADR route, AML obligations. |
| C09 | Remaining functionality and defects | ENGINEERING DONE with listed follow-ups | Three audits (store, regulatory, security) found verified defects; all fixed with tests in seven workstreams (see Claude checkpoint). Known open: intermittent cross-tab session e2e test; admin UIs for investment partners and loan review (gated features); off-plan rejection reason not shown to the developer. |
| C10 | Release evidence | ENGINEERING RECORDED; EXTERNAL pending | Test, build and export evidence in the Claude checkpoint. Open: DPC/licensing evidence, legal review, production configuration and data migration, store submission outcomes. |

## Claude checkpoint — 25 September 2026

Owner from this date: Claude (continuing Codex's ledger). Three read-only audits (store review, Ghana regulatory mapping, API security) found verified defects beyond the rows below; they were fixed in parallel workstreams and merged into `main` locally. Nothing has been pushed: Render auto-deploys `main`.

### Regulated activities (C07)
- Server-side gate `config/regulatedFeatures.ts` + `middleware/regulatedFeature.ts`: rent_collection, wallet, lending, financing, investments, insurance, payroll, credit_reporting. Production enables none unless `REGULATED_FEATURES` lists them and `REGULATED_BASIS_<FEATURE>` records the licence/partner basis, or boot fails. Routes 403 `FEATURE_UNAVAILABLE`; savings auto-debit stops; clients read `GET /api/platform/features` and hide/refuse screens (unknown = off); passport omits credit score; financier/employer sign-up withheld. Runtime-verified in production mode on a spare port. Doc: docs/compliance/regulated-features.md.
- Simulated payments refused in production unless `ALLOW_SIMULATED_PAYMENTS=true`; mode checked at boot.
- Real firm names removed (Databank, Epack, SIC, GLICO, Enterprise, MTN, UCC, Stanbic in demo data); insurance never seeded in production.
- Loans: lender/admin-funded disbursement only, human review, one open loan (unique index), ≥3-month term, server APR/total cost, acceptance recorded. Investments: partner-confirmed orders and settlements only. Insurance: verified insurer licence, insurer-owned claims within cover. Financing: Act 220 s.25 advance cap via `maxAdvanceMonthsFor`, funded disbursement, licence-verified financiers, signature evidence. Payroll: employee-accepted employment, mandate caps.
- Estate agency: REAC licence number on agency profiles, admin verification page, public page shows status, no owner id, moderated listings only.

### Security loopholes fixed
Loan self-disbursement money creation; invitation super_admin escalation via Mongoose object cast; marketplace settlement by reused payment reference; draft agreements creating fake tenancies (contact, credit, disputes, penalties, reviews); move-out zero-refund during dispute; profile-access contact leak; verification surviving identity edits; role changes not revoking sessions; dispute upload before authorisation; landlord-set lifecycle statuses; self-priced featured listings; author posts on the official blog; coupon burning; self-set booking prices; storefront domain squatting; webhook DNS-rebinding SSRF; payout timeout refunds.

### Privacy and consent (C02, C03)
Terms/Privacy/18+ acceptance recorded with versions, time, IP, UA; re-accept banner; invitations validated. Notification preferences enforced per channel/category with unsubscribe links. Privacy notice and Terms rewritten from the processor inventory; unverifiable claims removed. Ghana Card numbers AES-256-GCM encrypted (`PII_ENCRYPTION_KEY`, boot-checked; backfill `src/scripts/encryptPiiFields.ts`); landlords see idVerified + last 4 only. Shared passports minimal; completion metric relabelled; auth events audited; login timing equalised.

### UGC and store readiness (C04, C06)
Report controls on listings, reviews, businesses, workers (mobile); text filter on chat and reviews with auto-reports; mobile MFA sign-in; rights-check public; deep links mapped; dead controls removed; brand icons/splash from the web mark; mobile splash matches web; Android permissions blocked; export compliance; remote build numbers; push asked in context; AI legal disclaimer; store-billing notice on deletion; public /support page. Submission guide: docs/compliance/store-submission.md.

### Verification (merged `main`, 25 September 2026)
- API: 1,542/1,542 tests across 175 files, with the opt-in MongoDB suites (`RENTOS_TEST_MONGO_URI=mongodb://localhost:28018/rentos_compliance_e2e`); typecheck and lint clean.
- Web and mobile: typecheck and lint clean. API and web production builds pass. iOS and Android Expo exports bundle.
- Mobile Expo-web Playwright: 114/114. Web Playwright against the isolated e2e stack: 163 passed, 0 failed, 64 skipped (specs that need other configs).
- Production-mode runtime check on a spare port: every regulated route returns 403 FEATURE_UNAVAILABLE; boot refuses a feature without a recorded basis, simulated payments, and a missing PII key.
- `npm audit --omit=dev`: 0 vulnerabilities in API, web and mobile.
- Known intermittent: `cross-tab-session.spec.ts:47` fails occasionally (about 1 in 10 local runs); instrumented repeats showed no token overwrite or refresh, so it is recorded as test timing, not an app defect.

### Production rollout (25 September 2026)
Deployed to Render (`rentos-api`) and Vercel (`userentos`) from `main` at d3cf714, 767249a, f8c628a and c1e5c6d. Each deploy went live on the new build.

**Render environment set via the REST API.**
- `PII_ENCRYPTION_KEY`: new; a backup is in the git-ignored `apps/api/.env.production`.
- `PAYMENTS_PROVIDER_MODE`: changed from `simulated` to `live`. Production had been completing checkouts without moving money. No Paystack keys are configured, so collections are refused until they are added.
- `CORS_ALLOWED_ORIGINS`: added `https://*.userentos.com`.
- `REGULATED_FEATURES`: left unset, so every regulated feature is off (confirmed live).

**Production data changes.**
- The one plaintext Ghana Card number was encrypted. Afterwards: 0 plaintext, and the value decrypts with the backup key.
- No profiles held religion or ethnicity data, and no accounts were marked verified without review, so nothing needed changing there.
- 8 blog posts and 8 legal articles were corrected. The loan and investment promotions were archived. Posts about savings, credit scores and mobile-money rent payments are hidden while those services are off.
- Render one-off jobs run without a shell, so each command must be a single argument with no spaces.

**Verification.** Two read-only multi-agent passes against production. The first found issues, which were fixed in the three follow-up deploys. The second confirmed:
- Regulated-feature gating works, and financier sign-up is refused.
- CORS: rejected origins get normal handling with no CORS headers; allowed origins and platform subdomains work; `Vary: Origin` is sent.
- API headers are hardened (no X-Powered-By).
- Parser and cast errors return generic messages, and malformed bodies are rate-limited.
- The web app sends security headers.
- Corrected content and site copy are live.
- There were no regressions in the web app's preflights (Authorization and Idempotency-Key headers).

**Still for the operator.**
- Add Paystack keys (or keep payments unavailable).
- Attach a wildcard `*.userentos.com` domain on Vercel for storefront subdomains; this needs DNS.
- Tighten DMARC from `p=none`.
- Set `SENTRY_DSN` / `REDIS_URL`.
- Point the web `VITE_API_URL` at `https://api.userentos.com`; it currently uses the Render hostname, which works but is less portable.
- Add real listings before App Review.
- Complete the registration, licensing and legal items in the table above.

### Still external or open
Ghana registrations (DPC, company details in LEGAL_ENTITY), licences or partner agreements for any regulated feature, lawyer review of Terms/legal articles, Twi filter word-list review, staffed moderation (24h) and human-review mailbox, store forms and review outcomes, production data migration (PII backfill, indexes), admin UIs for investment partners/loan review (gated features).

## Source register

Read official sources during this audit; recheck at release because laws and platform policies change.

- Ghana Parliament: [Rent Act, 1963 (Act 220)](https://ir.parliament.gh/handle/123456789/2063). Initial PDF fetch returned 403; the later receipt/advance checkpoint below records the successfully retrieved Parliament bitstream and review of sections 25(5) and 33. Full corpus and current amendment review remain outstanding; do not treat the entire curated corpus as independently verified law.
- DPC: [Organisation obligations under Act 843](https://dpc.gov.gh/for-organisations/). Registration and operational data protection duties require evidence beyond code.
- Apple: [App Review Guidelines](https://developer.apple.com/app-store/review/guidelines/). Includes UGC safety, payments, privacy, account deletion and financial-service requirements.
- Google: [Account deletion requirements](https://support.google.com/googleplay/android-developer/answer/13327111). In-app request and an accessible external web resource are required for covered account-creating apps.
- BoG: [Digital credit licensing notice](https://www.bog.gov.gh/notice/publication-of-directive-for-digital-credit-services-providers-and-licensing-requirements/) and [licence categories](https://www.bog.gov.gh/fintech-innovation/licence-categories/). Determine scope against RentOS financing and wallet operation; a payment integration is not evidence of a platform licence.
- SEC Ghana: [Online investment and intermediary platform directive](https://sec.gov.gh/directive-to-market-operators-fintech-service-providers-and-persons-owning-and-or-operating-online-investment-and-trading-platforms/). Determine applicability to investment features and obtain registration evidence where required.
- REAC: [Real Estate Agency Act 1047](https://reac.gov.gh/reac-actpdf/). Verify agent/broker licensing and relevant transaction obligations.

## Initial confirmed findings

- Agreement controller cited `Rent Act, 2024 (proposed)` for a binding restriction.
- Application approval created pending agreements with empty compliance flags; signing trusted stored flags instead of current terms.
- Agreement updates allowed expired/terminated/disputed records to return to draft.
- Date strings accepted impossible or reversed tenancy periods.
- Special conditions were excluded from clause checks.
- Privacy policy claimed end-to-end encryption, unverified partner custody and a placeholder contact phone.
- Native settings did not expose account deletion or personal-data export.
- Old Phase 1 auth checkbox is stale: change-password already uses authenticate middleware.

## Completion rule

Each row needs implementation paths, relevant tests and runtime evidence before engineering completion. External registration, licensing, legal classification, production operation and store approval stay explicitly pending until verified. Do not mark the overall goal complete while required work remains.

## Verification checkpoint — 13 September 2026

Implemented and verified in this slice:

- Shared agreement checks on model validation and immediately before signing, including legacy unflagged records; impossible/reversed dates and advances above six months block signing. Special conditions receive the same limited clause review as ordinary terms. Expired, terminated, disputed and active agreements cannot be edited back to draft.
- Corrected proposed-law citation and removed blanket legal certification wording from generated agreement documents. Full statutory corpus and short-tenancy review remain open.
- Web Settings → Privacy and mobile Settings → Privacy expose data export and explicit typed-confirmation account deletion. Public `/delete-account` supports sign-in and email requests when account access is lost. Links to privacy/terms are present. Public endpoint deployment and mailbox operation require release verification.
- Deleted/missing accounts cannot use existing session or download tokens, or establish new sockets. Local connected sockets are disconnected on deletion. Biometric/push tokens and MFA secret are removed; refresh tokens revoked. A distributed socket deployment still needs shared-adapter verification.
- Removed false restoration promise, placeholder phone and end-to-end encryption claim. Related collection/upload erasure, accurate provider/retention disclosures and comprehensive export coverage are still open; this is not full C02 completion.
- API OpenAPI documentation now describes the supported export groups and closure consequences.

Checks: 569/569 API tests; API/web/mobile typechecks and lint; web/API production builds. Added 12 agreement regressions and three deleted-account middleware regressions. Existing authenticated route fixtures now explicitly mock a persisted active account instead of relying on JWT-only authentication.

Browser verification: 2/2 new Playwright tests passed against isolated MongoDB `rentos_compliance_e2e` on local ports 5574/3502. Tested public deletion access, JSON download, confirmation, account closure, and 401 rejection of both old access and refresh tokens. Email/SMS provider credentials were explicitly blank for this run. The initial attempt collided with a concurrently generated type file and server restart; the uninterrupted rerun passed.

Native bundle export: Android, iOS and Expo web passed (`npx expo export --platform all --output-dir /tmp/rentos-compliance-mobile`). Device/simulator interaction and full regression E2E remain pending. No production deployment or store submission performed.

Next bounded work: finish privacy inventory/retention and erasure reliability, then user reporting/blocking and store billing/permission review. Known remaining implementation gaps from the latest spec audit include Paystack settlements reconciliation and actionable user reports/account suspension. Keep these separate from the older stale unchecked roadmap items.

## Erasure reliability checkpoint — 13 September 2026

- Extracted scheduled cleanup into `services/accountErasure.ts`. Each account is checked for eligibility before related data is touched; the deletion cutoff is checked again during final account removal.
- Cleanup waits for every operation to settle. If any operation fails, the account tombstone remains for the next scheduled retry. Other accounts continue independently, with bounded 100-account keyset pages so failures at the front of the queue cannot starve later requests.
- Conversation previews and the deleted user's unread counters are erased alongside sent messages. Removed the incorrect claim that retained contracts and financial records become anonymous merely because the User record is deleted.
- Replaced three source-string deletion assertions with five behavioral tests covering eligibility, preview removal, partial failure with in-flight operations, retry, unmatched final delete and batch progression.
- Validation: 571/571 API tests and API lint pass. Real MongoDB verification used a uniquely named temporary local database, confirmed due-account removal and recent-account preservation, preview/profile/message cleanup and idempotency, then removed that temporary database. Typecheck initially found a union of Mongoose write-result types; the operations now share a PromiseLike<unknown> contract ; final API typecheck and production build pass.

C02 remains incomplete: identity/supporting uploads, further role-specific records, retained record classification and provider erasure still need end-to-end coverage. The worker's verified scope is the explicit operations in accountErasure.ts, not every personal-data collection.

## Stored document erasure — 13 September 2026

- New document uploads and versions persist server-returned storage public IDs and resource types. Explicit document deletion now removes the remote asset before deleting its database record, retaining the identifier on failure so the request can be retried.
- Cloudinary destruction requests use CDN invalidation and require `ok` or idempotent `not found` acknowledgement. Unknown results are failures. Reference: [Cloudinary asset deletion](https://cloudinary.com/documentation/delete_assets) and [Destroy API](https://cloudinary.com/documentation/image_upload_api_reference#destroy_method), checked during this implementation. CDN propagation and provider backups require operational verification.
- Legacy document identifiers are recoverable only from original URLs in the configured Cloudinary account's `rentos/documents` namespace. Foreign accounts, transformations and ambiguous paths require manual review rather than guessing an identifier. External document links are removed as records, but their third-party storage cannot be erased by RentOS.
- Scheduled account erasure processes identity documents and unlinked miscellaneous documents before discarding the account. Contract, receipt, notice, evidence and linked miscellaneous files remain for explicit retention review. This is an interim classification, not a claim that indefinite retention is lawful.
- Added 11 behavioral tests for resource types, CDN invalidation, idempotent acknowledgements, provider rejection, database retention on remote failure, retry and legacy ownership/path checks. A real MongoDB run with mocked provider transport verified selection boundaries (including another owner's files) and idempotency in a uniquely named temporary local database, removed after the test.
- Avatar and generic uploads still need ownership metadata and retryable asset cleanup; old tenant-profile URLs must not be treated as proof of storage ownership. Live Cloudinary deletion, CDN eviction and backup retention remain externally unverified. C02 stays IN PROGRESS.

Validation for stored-document slice: 582/582 API tests, API typecheck, lint, production build and git diff --check pass. Provider transport was mocked; no live customer assets were deleted.

## Avatar ownership and erasure — 13 September 2026

- Avatar uploads now write `AvatarAsset` before calling storage, using a generated identifier and the authenticated owner. Upload failures and replaced photos remain traceable for account erasure instead of losing their storage identifiers.
- The previous photo URL is recorded idempotently before replacement or account closure clears it. Legacy original URLs use the same conservative ownership/path recovery as document assets, scoped to `rentos/avatars`; third-party linked avatars can only have their references removed.
- Account erasure removes avatar assets first and keeps their records until storage confirms deletion. Configuration changes to another storage account block deletion for review. A concurrent account closure prevents an in-flight photo upload from repopulating the cleared profile.
- Five behavioral regressions cover durable ordering, database failure before upload, idempotent legacy tracking, failed remote deletion/retry and storage-account changes. Real MongoDB verification with mocked provider transport confirmed history cleanup, owner isolation and idempotency in a temporary local database that was removed afterwards.
- Limitations: uploads predating this change that were already replaced or whose URL was already cleared need a provider inventory/backfill; generic uploads and tenant-profile attachments still need ownership/retention mapping. Live CDN and backup erasure remain externally unverified. Replaced avatar files are currently erased at account cleanup, not immediately at replacement; an orphan-expiry policy remains to be implemented.

Avatar slice validation: 587/587 API tests, API typecheck, lint, production build and git diff --check pass.

## Tenant profile minimisation and save contract — 13 September 2026

- Removed religion and ethnicity inputs from profile editors; the API rejects new nonempty values and allows explicit clearing. Approved shared-profile responses omit historical values. Historical owner access/export remains available pending retention review.
- Religion no longer contributes to profile completion. Signing recomputes completion rather than trusting an old stored flag. Broader mandatory demographics, references, verification and first-time-renter requirements still need necessity and workflow review; this is not a completed data-minimisation audit.
- Fixed web saves submitting read-only response metadata to the strict PATCH endpoint, and mobile saves submitting unsupported smoking/pet aliases. Both use a shared editable-field picker; mobile maps its choices to the server booleans.
- Persisted the income currencies and sources already offered by the editors. Totals include only the selected currency and explain that conversion is not performed. Financing snapshots/displays preserve declared currency; passport GHS bands and developer GHS income averages exclude foreign-currency amounts.
- Source context: [DPC compliance principles](https://dpc.gov.gh/compliance/) and [individual information rights](https://dpc.gov.gh/for-individuals/), checked 13 September 2026. These changes reduce unnecessary collection; they do not certify every profile field or purpose as lawful.
- Five new contract/scoring tests pass. Full API suite: 592/592; API/web/mobile typechecks and lint pass. Browser suite: 3/3 passed, including save/reload with preserved income currency, prohibited-field rejection, export and account closure. The first browser attempt was obstructed by the new-user tour; the test now dismisses that tour through its visible control before editing. Native form interaction remains unverified on device.
- Browser runs also exposed a separate development configuration defect: `useSocket.ts` defaults to port 3002 when `VITE_SOCKET_URL` is unset, while this isolated API runs on 3502. Socket authentication warnings therefore came from another local endpoint; these browser passes do not verify realtime session behavior. Align the socket test target and add a connect/closure regression in the next slice.

## Realtime account lifecycle — 13 September 2026

- Fixed the preceding socket-target defect. The web socket follows `VITE_SOCKET_URL` when explicitly set, otherwise the HTTP API origin; relative APIs use a Vite `/socket.io` proxy to the configured API server. No fixed client-side development port remains.
- Server-initiated disconnects stay disconnected. Network/transport failures retain normal automatic reconnection. A shared socket updates its authentication and reconnects when HTTP refresh rotates the session token.
- Added a browser/device-client regression using real Socket.IO and local MongoDB: connect two sessions, refresh and reconnect the browser session, close the account, verify both disconnect, then reject explicit reconnection with the deleted account's token. All four focused browser tests pass, including refresh coverage. Web typecheck passes. Vite logged transient connection-reset/broken-pipe messages during development socket teardown; successful connection and terminal account-closure assertions were verified independently.
- Implementation references: [Vite server proxy](https://vite.dev/config/server-options.html#server-proxy) and [Socket.IO client lifecycle](https://socket.io/docs/v4/client-socket-instance/), checked 13 September 2026. Production reverse-proxy routing and distributed socket adapters still require deployment evidence.
- C04 reconnaissance confirmed reports currently cover property/storefront/blog/review targets; user/message reporting and bilateral user blocking were not found in the current API. Implement and verify those flows next, including web/mobile controls and moderation actions.

## Unwanted contact controls — 13 September 2026

- Added persistent directional `UserBlock` records; either direction prevents creation of conversations and new messages. Only the blocker can remove their own record; reciprocal blocks are independent.
- Blocked relationships are excluded from contact search and presence disclosure, and existing shared socket rooms are cleared when a block is placed. Existing message history remains available. Web and native chat offer block/unblock controls and disable sending for blocked relationships.
- Scheduled account erasure removes block records involving the erased account; personal-data export includes only the requesting user's own blocks. Contact-change events refresh both clients and rejoin the conversation after unblocking, subject to a fresh server block check.
- Validation: 592/592 API tests passed; the combined five-test browser suite passed. A subsequent expanded blocking test passed with account-export inclusion and visible delivery after unblocking, plus bilateral denial, search exclusion, history access, block ownership and reciprocal block behavior. Final API/web/mobile typechecks and lint pass; git diff --check is clean. Native interaction and concurrent block/send ordering across multiple API instances still need dedicated verification.
- Policy context: [Apple UGC guidelines](https://developer.apple.com/app-store/review/guidelines/#user-generated-content) and [Google Play UGC policy](https://support.google.com/googleplay/android-developer/answer/9876937), checked 13 September 2026. Both require reporting and blocking protections for relevant UGC experiences; blocking alone does not complete C04. User/message reporting, moderation actions and filtering are outstanding.

## Private-message reports — 13 September 2026

- Extended report targets to messages, with conversation-membership checks before exposing or accepting a private target. Outsiders receive no target details, senders cannot report their own message, and the existing unique open-report constraint prevents duplicates.
- Added web report forms (reason and optional details) and native reason selection on incoming messages. Successful submission confirms receipt; reporting remains separate from blocking.
- Existing admin resolution API can remove reported messages. History responses return a removal notice, matching conversation previews are cleared, and participant sockets trigger refetch. The original message and report snapshot remain restricted moderation evidence pending the broader retention review; this is removal from conversation display, not erasure.
- Validation: 597/597 API tests; API/web/mobile typechecks and lint; five browser tests including report submission, outsider denial, self-report denial, duplicate protection and reporter status access. A uniquely named temporary MongoDB verified participant checks, persisted removal, preview cleanup and actual history-controller masking, then was dropped. No external messages or customer data were used.
- C04 remains IN PROGRESS: usable admin report-queue UI, user/account reports and suspension, preventive content filtering, moderation staffing/response procedures, historic notification copies and native-device interaction need completion. Race handling for simultaneous send/block/moderation also requires dedicated verification.

## Admin moderation queue — 13 September 2026

- Added `/admin/content-reports` and an admin/super-admin sidebar entry. The queue displays reported evidence, reporter context, duplicate counts, status filters and pagination; moderators can claim a report, supply a decision reason, remove content or dismiss it, and inspect resolved records.
- Resolution now requires a claimed report assigned to the acting moderator. Previously another moderator could resolve an already claimed report. Terminal reports remain protected against re-resolution. The UI does not offer the legacy `warned` action because delivery of that warning is not implemented.
- Validation: 597/597 API tests; API/web typechecks and lint; dedicated Playwright moderation flow passed against the isolated local database. Verified non-admin denial, unclaimed-report rejection, assigned-moderator ownership, browser claim/removal, persisted decision display, recipient history masking and terminal-state rejection. Test-only role fixtures are restricted to the explicit local compliance database and newly created accounts; those accounts were closed afterwards.
- Remaining moderation work: claim release/reassignment and concurrent resolution safety, account-level reports/suspension, preventive filtering, complete native/admin operational access and staffed response evidence. This checkpoint does not complete C04 or store readiness.

## Moderation decision recovery — 13 September 2026

- Added claim release: the assigned moderator or a super admin can return an undecided report to the open queue. The admin UI exposes release where authorized, and releases are audited.
- Decision action/reason are durably reserved with an atomic condition before content removal. Competing decisions are rejected; identical retries can complete an interrupted idempotent removal. Finalization uses an atomic status/owner/decision condition, so only one request records completion. Duplicate-report closure skips reports with a reserved decision.
- Failed requests refresh the queue; reopening a pending report presents its saved decision read-only for retry. If the target was already missing before removal, the reservation is cleared so the moderator can document a dismissal. The API no longer accepts the unimplemented `warned` action for new resolutions; historical records remain readable.
- Validation: 597 API tests pass; API/web typechecks and lint pass. Expanded real-browser/API test passes with release ownership, release/reclaim, conflicting simultaneous decisions (one 200, one 409), durable-decision conflict rejection and interrupted-removal retry, in addition to the existing moderation workflow.
- Recovery of reserved decisions belonging to departed moderators, exhaustive fault injection after each persistence step, distributed block/send ordering, account suspension and preventive filtering remain open. C04 stays IN PROGRESS.

## Abandoned-claim recovery — 13 September 2026

- Super admins can take over another moderator's active claim from the queue. The API atomically changes ownership only while the report is still reviewing, leaving any reserved decision and reason intact. Ordinary admins are rejected; completed reports cannot be taken over.
- After transfer, the prior owner cannot finalize the report. The new owner sees the saved action/reason as read-only and can complete the same decision. The existing best-effort audit writer records the prior owner and pending action; guaranteed audit delivery remains an operational gap.
- Expanded real-browser/API regression passed: ordinary-admin denial, super-admin takeover from the queue, preserved/read-only decision, previous-owner rejection and successful finalization by the new owner. API/web typechecks and lint pass; git diff --check is clean. Test accounts were confined to the explicit local compliance database and closed afterwards.
- Next: account-level report/enforcement workflows and preventive filtering. Exhaustive failure injection, guaranteed audit delivery, native-device checks and external moderation-response evidence remain open; the overall goal is active.

## Account reports and suspension — 13 September 2026

- Added user reports to web/native conversation controls and the moderator queue. A user report supports dismissal or account suspension; message/content reports cannot invoke account suspension. Staff accounts are protected from this moderation path.
- Suspension persists its originating report and reason, invalidates ordinary authenticated access (including existing/new session tokens), rejects optional-auth privileges and socket connections; download access is narrowed to owned tenancy PDFs in the next checkpoint, and disconnects existing sockets. New contact search and message delivery exclude suspended recipients.
- Account details, export, deletion and selected account-security controls remain available. Login remains available for this restricted access. Web/native notices link to privacy controls and the support address for appeal or existing-obligation assistance. Support operation, appeal service levels and access to contractual/financial obligations require release review; the email link alone is not proof of an operational appeal process. Scheduled financial jobs and public historical content are not automatically removed or cancelled by suspension.
- Moderators can restore access with a reason, tied to the originating suspension report so an old decision cannot lift a newer suspension. Both suspension and restoration are recorded through the existing best-effort audit mechanism; guaranteed audit persistence remains open.
- Six browser tests passed across chat safety, privacy and moderation, including suspension, restoration, stale-restoration rejection, restricted login, export and deletion while suspended. A subsequent expanded moderation test verifies live socket disconnection/reconnection rejection. Additional middleware/service tests cover privacy/security exceptions, prohibited requests, protected accounts and races that make suspension ineligible. Final validation: 611/611 API tests; API/web/mobile typechecks and lint; git diff --check. The final test-mock typing correction was rechecked with 25 focused auth/suspension tests.
- C04 remains IN PROGRESS: preventive content filtering, public/historic content handling, notification copies, operational appeals, obligation access, native-device testing and distributed concurrent-enforcement coverage remain unresolved.

## Existing obligations during suspension — 13 September 2026

- Suspended sessions can read their own agreements and payment history, obtain/download their tenancy PDFs, inspect available payment methods and initiate rent payments for their active agreements. Creating/editing/signing agreements and unrelated routes remain blocked. Existing privacy/security exceptions remain intact.
- Middleware derives suspension status from current account records. Agreement and payment controllers suppress global role access during suspension; payment controllers also suppress delegated access. PDF routes independently enforce agreement-party ownership for both link issuance and download.
- Web/native suspension notices link directly to agreements, rent payments and privacy controls, retaining the appeal/support contact. Other outstanding obligations (for example a terminated agreement, service order or financing balance), operational appeal handling and native-device interaction still require further work. This is a bounded access correction, not a claim that every contractual obligation is supported.
- Validation: 625/625 API tests; API/web/mobile typechecks and lint; two focused browser tests for moderation and obligation access. An expanded obligation regression also passed, proving active delegation grants payment access before suspension and loses it afterwards. Coverage includes owned/foreign records, PDF ownership, simulated rent collection, draft-payment denial and suspended government-role isolation. git diff --check passed. Native device and live payment-provider behavior are not verified by these checks.

## Native payment inventory and purchase defects — 13 September 2026

- Rechecked primary policies: [Apple 3.1.1 / 3.1.3](https://developer.apple.com/app-store/review/guidelines/) and [Google Play Payments](https://support.google.com/googleplay/android-developer/answer/9858738?hl=en). Rent and qualifying physical services use external payment rails; listing-plan functionality requires a separate digital-purchase classification. No regional billing exception or companion-app eligibility is evidenced here.
- Confirmed release gap: `apps/mobile/app/subscription.tsx` offers paid listing plans through `/subscriptions/subscribe` and mobile money/bank transfer. Registration leads paid selections to this screen. No StoreKit/Play Billing client, store product mapping, purchase verification or restoration is present in the examined native manifest/API subscription path. The existing paid checkout remains implemented but is NOT approved for store distribution by this audit. Keeping that gap open does not imply an accepted exception.
- Next C05 implementation: native store purchase integration and product mapping, server-verified platform transactions tied to the authenticated account, idempotent entitlement grants, renewal/expiry/refund/revocation reconciliation, restoration and subscription-management UI. Verify storefront-specific requirements and actual App Store Connect/Play Console products, credentials and sandbox transactions. Inventory promotion and other digital upgrades separately; do not repurpose the rent rail or accept a client-only purchase claim.
- Fixed concrete payment defects: native rent and subscription pickers now load backend-available payment methods, clear stale selections and show unavailable/load-failure states. Subscription API refuses an unavailable rail before creating a payment or contacting a provider, preserving read-only idempotent retries. Native rent payment displays returned provider instructions, including bank transfer, as selectable screen text as well as its alert.
- Fixed native subscription response mismatch (`features` versus the actual `benefits` field), yearly price-period labeling and the unsupported promise of automatic renewal (`Ends` now reflects the stored expiry). This repair does not add store billing.
- Verification: 16 focused API tests pass, including unavailable-subscription-rail rejection before side effects. Three Expo-web component-flow tests pass with mocked API contracts: yearly package/benefits and available methods, bank instructions and failure-state behavior. API/native typechecks and lint pass; git diff --check passes. These are not device, live-provider, store-sandbox or production tests. Payment instructions remain session-local; durable recovery of collection instructions needs review.

## Store catalogue and purchaser binding — 13 September 2026

- Added `StoreProduct` with unique platform/product/base-plan mappings. Mappings are created inactive, can enter or leave the catalogue, and cannot be reassigned or deleted through the API. Package deletion is refused while any store mapping references it. Google mappings require a base plan; Apple mappings reject that extra field. Catalogue entries include only active paid packages and active mappings, with no web price presented as a store price.
- Package editor now provides permission-gated store mapping controls. It clearly distinguishes mapping from creating a store product or enabling verified native purchasing. Actual Store Connect/Play Console identifiers must be supplied by the operator; only isolated fixture identifiers were created in tests.
- Added `/api/store-billing/catalog?platform=apple|google`, `/account` (POST), and permission-gated `/products` administration. Account allocation persists a random UUID once on the authenticated User; concurrent requests return the persisted winner. Apple receives this UUID and Google receives a namespaced SHA-256 pseudonym. Request-supplied user IDs cannot change the binding. Identifiers are hidden from ordinary profile responses, included in self-export, and removed with the User by existing erasure. Future verified receipts and their retention still need their own lifecycle design.
- Primary references: [Apple appAccountToken](https://developer.apple.com/documentation/appstoreserverapi/appaccounttoken), [Google purchase verification and account mapping](https://developer.android.com/google/play/billing/security), [Google obfuscated account ID](https://developer.android.com/reference/com/android/billingclient/api/BillingFlowParams.Builder). Account identifiers associate purchases; they do not establish that a payment occurred.
- Validation: API/web typechecks and lint passed. Real local API/database/browser regression passed for admin mapping creation/availability, immutable associations, duplicate rejection, protected package deletion, platform field validation, catalogue contents, five concurrent account allocations, separate users, privacy export and post-closure rejection. All 626 API tests and git diff --check passed.
- C05 remains IN PROGRESS. Native SDK purchase flow, signed/provider-verified receipts, entitlement persistence, restoration, subscription lifecycle/revocations and store sandbox evidence are NOT implemented by this foundation. Product/package deletion races and versioned entitlement snapshots require attention when implementing transaction grants. No store products, purchases or external accounts were created by this test.

## Google Play verification adapter — 13 September 2026

- Added `services/storeBilling/googlePlay.ts` and a direct `google-auth-library` dependency. It fetches subscription state from the fixed Google Publisher `subscriptionsv2.get` endpoint using a server-configured package name and scoped service-account file. The purchaser token is URL-encoded and is not returned or included in propagated errors. No client receipt JSON is accepted by the transport entry point.
- Normalization checks account binding, known states, timestamps and base-plan identity. Active/grace/canceled-but-unexpired items may be access-eligible; pending/paused/on-hold/expired items are not. Future deferred replacements are excluded; absence of an order ID alone does not reject promotional purchases. Linked tokens are hashed for subsequent replacement reconciliation; personal Google profile fields are discarded.
- Test receipts require explicit non-production opt-in and are always rejected with NODE_ENV=production. Missing config fails before a network call. Publisher errors are converted to sanitized categories, with 404/410 treated as invalid purchases; raw Gaxios errors can expose secrets and are not propagated. The publisher request has a 10-second timeout and no automatic request retry; credential acquisition behavior remains owned by the auth library.
- References: [subscriptionsv2 resource](https://developers.google.com/android-publisher/api-ref/rest/v3/purchases.subscriptionsv2), [server GET endpoint](https://developers.google.com/android-publisher/api-ref/rest/v3/purchases.subscriptionsv2/get), [Google purchase verification guidance](https://developer.android.com/google/play/billing/security). Granting access, linked-token revocation, acknowledgement after grant and voided-purchase handling still require the durable workflow. The verifier is internal and has not been exposed as a completed purchase endpoint.
- 25 focused tests pass using mocked authenticated transport, covering state eligibility, expired/future/invalid dates, binding, deferred versus promotional items, response minimization, endpoint/credential scope, configuration, test mode and error sanitization. No actual Google purchase, Play Console credential or sandbox transaction was used. All 651 API tests, API typecheck, lint and git diff --check passed. C05 remains IN PROGRESS; next implement durable verified-purchase/entitlement state and acknowledgement/reconciliation, then Apple verification and native billing integration.

## Renewal-safe expiry processing — 13 September 2026

- Before introducing store entitlement persistence, found that the current expiry job updates users by ID alone after reading an expired snapshot. A concurrent paid renewal or reassignment could be overwritten. The free-plan cleanup branch could similarly clear a newly granted expiry, and the default-plan lookup did not require a free package.
- Added `services/subscriptionExpiry.ts`. Writes compare the observed package ID and expiry and exclude deleted accounts. Paid expiry pins the free fallback version; free-plan date cleanup uses the same concurrency guard. Missing/malformed packages retain their expiry for review. The scheduler selects an active default with price zero, notifies only after an actual downgrade, and isolates per-account failures for retry.
- Nine focused tests passed. A real isolated MongoDB check (`/tmp/rentos-expiry-check.ts`) verified renewal preservation, ordinary expiry downgrade, fallback version and idempotent retry, then removed its fixture records. The first attempt failed because fixture packages omitted unique slugs; after correcting fixtures, all runtime assertions passed. All 660 API tests, API typecheck, lint and git diff --check passed.
- This repairs the existing expiry consumer; it does not implement store entitlements or acknowledgement. Cross-document package-edit races, immutable commercial snapshots and provider-driven renewal/revocation remain part of the pending store workflow.

## Durable Google purchase observations — 13 September 2026

- Added `StorePurchase` and `recordGooglePurchase`. The service resolves the active user's existing store account binding, fetches a fresh Google observation and writes a unique application/token-hash record with an immutable owner. It rejects another owner's record before verification. First-write duplicates and revision conflicts require fresh verification; previously fetched data is never blindly retried against a newer revision.
- Reconciliation tokens are encrypted with AES-256-GCM and random nonces. Authenticated context includes platform, application, token hash and account, preventing ciphertext substitution across records. The dedicated 32-byte key has no default or JWT-secret fallback. Ciphertext is query-hidden and omitted from the returned journal record and self-export; normalized purchase metadata is included in self-export.
- Records currently remain in `entitlementState=pending`. This is a verified-observation journal, not a completed purchase endpoint: no User entitlement is changed and Google acknowledgement is not sent. Product-to-versioned-entitlement resolution, linked-token revocation, granting, acknowledgement retries and provider-driven reconciliation remain required before wiring native purchases.
- Thirteen focused tests passed for encryption/nonces/tampering/context/key mismatch, ownership, provider failure, account binding and revision conflicts. An explicitly isolated Mongo integration test passed for encrypted persistence, one winner under concurrent updates and fresh-verification retry; its records were cleaned up. Google transport remains mocked, not sandbox-verified.
- Privacy/release dependencies remain open: retention and erasure classification for this financial journal, key backup/rotation/migration, account-closure races before entitlement grants, operational recovery and store sandbox transactions. The encryption key must be retained for existing ciphertext until a controlled migration; changing it alone makes existing reconciliation tokens unreadable. No purchase API is exposed yet. All 674 API tests passed with the isolated Mongo integration enabled; API typecheck, lint and git diff --check passed.

## Frozen store entitlement terms — 13 September 2026

- New store mappings capture a complete entitlement snapshot: plan identity/version/name, registry defaults plus explicit grants and legacy limits/fees, billing cycle and benefits. Primitive grant types and numeric finiteness are checked before capture. The mapping schema makes the snapshot immutable; the update API still accepts only availability changes.
- The native catalogue now reads captured terms rather than mutable package values. Package activity/paid eligibility still controls whether a mapping is offered for new purchases. Existing mappings without snapshots are excluded and cannot be activated; the admin UI identifies them for review rather than silently assigning today's terms to historical purchases.
- Fixed inherited-property acceptance in the feature registry (for example `constructor` and `__proto__` are not capabilities). Unsubscribed fallback resolution now selects an active default with price zero, consistent with the expiry job's free-fallback rule.
- Validation: 16 entitlement tests, including snapshot isolation and invalid/inherited feature cases; expanded real browser/API/database mapping test proves later package/version changes do not change captured/catalogue limits. API/web typecheck and lint passed. Full API suite passed 679 tests with isolated Mongo integration enabled; the final free-fallback filter change was then rechecked with the 16 focused entitlement tests. git diff --check passed.
- This captures future store-purchase terms; it does not retroactively reconstruct historical legacy subscriptions or activate the pending journal. Legacy column/grant version integrity, historical migration, product-to-purchase matching, entitlement activation, linked-token revocation and acknowledgement remain open. No store receipt or sandbox transaction was processed.

## Product matching and grant preparation — 13 September 2026

- Added `prepareGoogleEntitlements`: it matches each still-eligible verified product AND Google base plan to the mapping's captured terms, validates snapshot identity/feature types and records a prepared grant set against the observed journal revision. Inactive mappings remain usable for restoration; missing mappings or invalid snapshots refuse the entire preparation rather than partially granting a multi-item purchase.
- Rechecks active account/eligible roles, app identity, production-versus-test environment, provider state, start time and expiry. Journal writes use owner/revision conditions; a newer provider observation resets preparation to pending and clears its grants and revision. Preparation remains separate from actual access and does not acknowledge purchases.
- Nine focused preparation tests pass, covering historical mappings, absent/malformed mappings, account/app/test restrictions, expiration, provider holds and revision conflicts. Expanded real-Mongo integration verifies prepared snapshot persistence and invalidation after a fresh observation; all 688 API tests passed with the isolated integration enabled. API typecheck, lint and git diff --check passed.
- Required next: activate prepared grants through the entitlement engine with current expiry/account checks, preserve linked-purchase supersession, implement acknowledgement retries and reconciliation, then expose the verified purchase flow to native SDKs. Prepared rows alone do not unlock features. Apple verification, store sandbox evidence and remaining C01–C10 work remain open.

## Store entitlement activation and domain access — 13 September 2026

- Added `activateGoogleEntitlements`, with current account/role, app/environment, prepared-revision, provider-state and expiry checks. Activation changes the journal to active or revoked; it does not rewrite the legacy web subscription. A replacement marker is recorded on successful replacement activation and survives later revocation, so old purchases cannot regain access simply because their replacement expires or is revoked.
- Entitlement resolution now reads active, unexpired prepared snapshots at their matching journal revision and excludes superseded tokens, including across account owners within the same application/environment. For this single-plan product, the most recently started valid store subscription is selected; a newer still-valid legacy subscription takes precedence. The subscription summary uses the same selection and captured limits. Property creation recognizes store access despite an older expired legacy subscription while preserving the existing expired-subscription guard when no store access exists.
- Extended isolated Mongo integration covers actual quota enforcement from the captured snapshot, suspended-account activation denial, stale activation rejection, replacement selection and non-resurrection after replacement revocation. Google transport remains mocked. The full suite initially caught the existing expired-legacy property policy; the implementation was adjusted to preserve it while permitting effective store access.
- Access is now implemented internally. A completed native purchase endpoint, Google acknowledgement/retries, renewal/revocation reconciliation, restore UI, Apple verification and sandbox/device evidence remain outstanding. Verification resets journal activation to pending; workflow crash recovery must complete the prepare/activate steps before this flow is exposed. Replacement-marker retention must survive appropriate account-erasure processing. C05 and the overall goal remain IN PROGRESS.
- Final validation: API typecheck and lint passed; all 688 tests across 67 files passed with `RENTOS_TEST_MONGO_URI=mongodb://localhost:28018/rentos_compliance_e2e`, including the expanded persistence/activation integration. `git diff --check` passed. These are local engineering checks, not a live store transaction or approval.

## Google acknowledgement and interrupted completion — 13 September 2026

- Added authenticated server-side acknowledgement transport using the configured application, encoded token/product, Android Publisher scope, empty payload and bounded timeout. Raw provider failures remain sanitized; HTTP retries are disabled so an ambiguous response is resolved through a fresh subscription verification.
- Added `completeGooglePurchase`: verify/persist, prepare captured terms, durably activate, then acknowledge an active purchase and record success against the same owner/revision. Already-acknowledged observations skip the POST. Pending/expired purchases without active grants are not acknowledged. Failed acknowledgement leaves the journal retryable; a later completion attempt re-verifies Google, so a previously successful POST with a lost response/local write can recover without another POST. Concurrent journal updates refuse the stale local acknowledgement write.
- Primary contract checked: [Google purchases.subscriptions.acknowledge](https://developers.google.com/android-publisher/api-ref/rest/v3/purchases.subscriptions/acknowledge). The adapter supplies the verified prepared product ID; Google's contract also allows omission of subscription ID for add-on subscriptions. RentOS native multi-item purchase UX remains unimplemented.
- The completion service is internal: automatic retry scheduling, operational failure visibility, reconciliation, native endpoint/SDK/restore, Apple verification and live sandbox evidence remain required. There is no claim of background recovery or a completed native checkout yet.
- Validation: all 698 API tests across 68 files passed with isolated Mongo integration enabled; API typecheck, lint and diff checks passed. Ten new tests exercise transport constraints, stage failures, a lost acknowledgement response, inactive purchases and concurrent observation conflicts. Provider calls are mocked; credentials and actual purchase acknowledgement remain externally unverified.

## Google purchase background recovery — 13 September 2026

- Added a five-minute scheduler task for interrupted pending/prepared purchases, active purchases awaiting acknowledgement and Google pending-payment observations. Each bounded batch claims rows atomically with a unique five-minute expiring lease; release uses the same lease identifier. A crashed holder can be retried after expiry. Due-time ordering and capped exponential backoff prevent immediate repeated attempts; one failed completion does not stop subsequent rows.
- The worker decrypts tokens only for the claimed attempt, checks the stored token hash, and runs the complete verification/preparation/activation/acknowledgement flow. Production scans exclude test purchases regardless of the test opt-in setting. Missing store configuration skips processing. Logs contain aggregate counts only; persisted failures use fixed categories, not provider exception messages or tokens.
- Added unit checks for selection, lease fencing, backoff/error isolation, token corruption, configuration gating and bounded batches, plus an isolated Mongo test of competing worker exclusion, delayed retry and expired-lease recovery. This recovery worker is separate from full renewal/refund/revocation reconciliation; successfully acknowledged active purchases still require that lifecycle work. Account closure/suspension/provider-invalid-purchase handling, operational alerting, native integration, Apple and sandbox verification remain open.
- Validation: all 704 API tests across 70 files pass with the isolated Mongo integration enabled. API typecheck, lint and diff checks pass. Provider completion is mocked in the worker integration; no live Google purchase or scheduler deployment was performed.

## Google subscription lifecycle polling — 13 September 2026

- Extended the leased worker to poll successfully acknowledged active subscriptions and grace, cancelled, on-hold and paused states, in addition to incomplete purchases. The existing verified-observation/preparation/activation path now refreshes renewal expiry and removes or restores access as provider state changes. Cancelled-but-unexpired and grace-period purchases retain eligible access; holds and pauses remove it. Completed expired or pending-cancelled observations stop scheduled polling, while an interrupted terminal observation can still finish preparation/activation.
- A new isolated Mongo integration exercises the actual completion, journal, matching, activation, scheduler-worker and effective-store selection services while mocking only Google transport. It covers initial acknowledgement, renewal without a second acknowledgement, grace, hold/recovery, pause/resume, cancellation before expiry, expiration and terminal polling exclusion.
- Primary lifecycle contract checked: [Google subscription lifecycle](https://developer.android.com/google/play/billing/lifecycle/subscriptions), which identifies subscriptionsv2.get as the authoritative state source and distinguishes access during grace from loss of access during hold. This polling fallback does not establish real-time notification delivery. The five-minute scheduler and bounded batches mean changes can be delayed; RTDN authentication/ingestion, refund/voided purchase handling, capacity/alerting, account lifecycle and native/Apple work remain open.
- Validation: all 705 tests across 71 API test files passed with isolated Mongo enabled; API typecheck, lint and diff checks passed. No real store transaction or deployed scheduler was exercised.

## Authenticated Google subscription notifications — 13 September 2026

- Added `POST /api/webhooks/google-play`. Google OAuth ID-token verification checks signature/lifetime/audience via the official client and explicitly checks the configured push service-account email, verified-email claim and Google issuer. Requests also require the exact configured Pub/Sub subscription resource and Android application ID. Configuration is explicit in `.env.example`; missing configuration fails closed.
- Subscription notifications prompt the existing fresh Publisher verification/completion flow; the notification type or event timestamp is never treated as authoritative entitlement state. A 204 is sent only after successful durable completion and a delivery deduplication marker. Markers contain only subscription/message identifiers and expire after 30 days. Repeated completed deliveries skip processing; concurrent completion-marker duplicates are harmless. No raw tokens or provider errors are logged or returned.
- Unknown purchase ownership or processing failure returns 503 for Pub/Sub redelivery. This supports events arriving before device purchase registration without guessing an account. Test notifications return 204 after authentication and application validation. Unsupported one-time/refund notifications remain non-success responses rather than false acknowledgements; refund/voided and pending-review workflows still require implementation and operational dead-letter handling.
- Required deployment evidence: configure an authenticated Pub/Sub push subscription targeting this exact endpoint, set `GOOGLE_PLAY_PUBSUB_AUDIENCE`, `GOOGLE_PLAY_PUBSUB_SERVICE_ACCOUNT_EMAIL` and `GOOGLE_PLAY_PUBSUB_SUBSCRIPTION`, connect the Play Console topic, configure retries/dead-letter monitoring, and demonstrate a real signed test delivery and sandbox state transition. None of these external actions has been performed.
- Primary sources checked: [Pub/Sub push authentication](https://docs.cloud.google.com/pubsub/docs/authenticate-push-subscriptions) for signed ID tokens and expected audience/service-account identity; [Google RTDN reference](https://developer.android.com/google/play/billing/rtdn-reference) for encoded envelopes, mutually exclusive notification types and mandatory follow-up provider reads.
- Validation: all 717 API tests across 72 files pass with isolated Mongo integration enabled; API typecheck, lint and diff checks pass. Twelve new tests exercise the real HTTP route with mocked ID-token verification/provider persistence boundaries. Cryptographic Google signing and actual Pub/Sub delivery remain externally unverified.

## Order-specific Google subscription refunds — 13 September 2026

- Authenticated full-refund subscription RTDNs now record the voided order ID on the matched purchase using an idempotent set before attempting a fresh provider completion. The matching app/token/account condition prevents applying evidence to another purchase. Provider refresh does not overwrite this evidence, and a failed refresh leaves delivery retryable.
- Grant preparation, activation and active entitlement reads exclude product/base-plan items whose current provider order is voided. Read-time exclusion prevents already-prepared access remaining usable while refresh fails. Renewal orders are distinct: a refund for an old order does not revoke a later paid renewal sharing the same purchase token. Duplicate and delayed events preserve this distinction.
- Expanded the HTTP notification tests and actual Mongo lifecycle integration to cover persisted refund evidence before a failed provider read, immediate access exclusion, retry/deduplication, later-renewal restoration and a delayed old-order refund. Only transport is mocked in the lifecycle integration; journal/mapping/activation/access services and Mongo writes run normally.
- Primary contract: [Google voided purchase notifications](https://developer.android.com/google/play/billing/rtdn-reference) identify subscription orders separately across renewals and provide sufficient order/purchase information for entitlement adjustments. One-time/quantity-based refunds, pending chargeback review operations, missed-notification reconciliation via the Voided Purchases API, financial retention classification and live sandbox refund evidence remain open.
- Validation: all 718 tests across 72 API files passed with isolated Mongo integration enabled; API typecheck, lint and diff checks passed. No live refund, signed Pub/Sub delivery or store submission was performed.

## Authenticated Google completion and restoration API — 13 September 2026

- Added `POST /api/store-billing/google/complete`, accepting only `{ "purchaseToken": "..." }` with a normal authenticated session. The existing landlord/property-manager role requirement, live account/suspension checks and write limiter apply. Caller-supplied account, package, price and entitlement fields are rejected. Both SDK purchase callbacks and restored owned tokens use this verified flow.
- Success returns the journal ID/revision, verified provider purchase state, entitlement state and acknowledgement flag, with `Cache-Control: no-store`. A successful HTTP response alone does not mean an active subscription: pending/expired provider states remain distinct and the client must inspect the returned state. Raw tokens, provider payloads and encryption data are absent.
- Ownership/account-binding failures return 403, invalid or disallowed-test purchases 422, revision conflicts 409 with a short Retry-After, and ambiguous processing/configuration failures 503 with a retry instruction that explicitly avoids another purchase. Raw exceptions never reach the global error logger. Added typed ownership/binding failures without changing the existing ownership restriction.
- HTTP tests exercise actual session authentication/role/suspension middleware with mocked completion, covering both eligible roles, denied roles/deleted/suspended accounts, token purpose, strict body validation, binding failures, pending state and sanitized retry responses. Existing isolated Mongo lifecycle tests cover the actual completion/persistence/activation services below this boundary.
- Native billing SDK integration, listener/restore UI, Apple implementation, missed-refund reconciliation, pending reviews, broader Ghana compliance items and external store evidence remain open. This API does not establish device or live-store verification.
- Validation: all 731 API tests across 73 files pass with isolated Mongo enabled; API typecheck/lint pass. OpenAPI includes the endpoint and its statuses; the final documentation addition passed API typecheck, targeted lint and diff checks. No native or live-store purchase was performed.

## Android native subscription integration — 13 September 2026

- Added pinned `expo-iap@5.6.0` and its Expo config plugin. An Android-only signed-in session component connects to Google Play, retains purchase listeners across navigation, restores on foreground and account initialization, and submits purchase tokens to the authenticated completion API. It checks account continuity around asynchronous work, keeps tokens out of app storage, and leaves acknowledgement to the backend after verification.
- Android subscription cards combine captured server entitlements with exact product/base-plan offers from Google. Prices, introductory phases and recurrence terms come from the SDK; missing pricing, mismatched base plans and unsupported installment offers are excluded. Added restore, reload, purchase status and Google subscription-management controls. Completed verification refreshes the current-plan summary. An existing/pending Google purchase is restored rather than starting another independent subscription; plan-replacement/upgrade UX remains open.
- Paid native subscription checkout no longer uses Mobile Money/web pricing. Web retains its existing flow. iOS explicitly reports App Store purchases unavailable pending Apple implementation. A current Google subscriber is not offered a free-plan switch that would leave recurring store billing misleadingly active. Physical rent payment flows remain unchanged.
- Validation: mobile typecheck/lint and API typecheck pass; Android and web JavaScript exports succeed. All six mobile Playwright checks pass: three existing browser-rendered billing regressions plus three SDK-price/base-plan mapping checks. These checks do not exercise native purchase events or a signed app. The prior 731 API tests remain the latest full backend result; this slice changes mobile code and test configuration.
- Required next: real Android development/store build and sandbox purchase, cancellation, pending-payment, account-switch, restore and interruption tests; plan replacements and unsupported offer types; Apple checkout/verification; subscription disclosures and device accessibility review; remaining Ghana and operational release requirements. `npm install` reported 38 dependency audit findings (1 low, 22 moderate, 15 high); dependency triage is still required, and these counts are not evidence that expo-iap introduced them.
- Sources checked: [Expo in-app purchases](https://docs.expo.dev/guides/in-app-purchases/) and the [maintainer repository migration/installation guidance](https://github.com/hyochan/expo-iap/blob/main/README.md). Implementation signatures were verified against the installed 5.6.0 package's generated types. Native billing requires a development/store build; JavaScript export alone does not verify native linking or billing service behavior.

## Mobile dependency and SDK compatibility repairs — 13 September 2026

- Updated the 12 packages identified by `expo install --check` to compatible SDK 55 patch versions, including Expo 55.0.31 and Router 55.0.18. Expo's installer registered the existing SecureStore module's config plugin. No framework major-version migration was performed.
- Applied non-force, semver-compatible audit fixes, then aligned Metro, metro-config and metro-transform-worker to 0.83.8 through explicit overrides. Expo already used this patch; the React Native CLI's parallel 0.83.7 tree retained a vulnerable image-size parser. The resolved dependency tree now uses Metro 0.83.8 consistently and no longer includes that image-size path.
- Current npm audit package findings decreased from 38 (15 high, 22 moderate, 1 low) to 21 (2 high, 19 moderate). These are dependency findings, including inherited chains, not a count of confirmed exploitable app paths. Remaining roots include the legacy Markdown renderer/markdown-it/linkify-it, navigation query decoding and xcode/uuid tooling. Automated proposals to downgrade Expo/Router or cross framework majors were not applied; remaining findings require explicit remediation and verification.
- Primary advisory evidence reviewed: [linkify-it scan complexity](https://github.com/advisories/GHSA-22p9-wv53-3rq4) and [markdown-it smartquotes complexity](https://github.com/advisories/GHSA-6v5v-wf23-fmfq). The mobile blog detail screen uses react-native-markdown-display, so the remaining renderer dependency is relevant and is not waived merely because other fixes pass.
- Validation after final dependency resolution: Expo compatibility check reports up to date; mobile typecheck/lint, Android and web exports, all six mobile Playwright checks and diff checks pass. The temporary Expo server was stopped. Native compilation/device testing and the remaining security findings are still open.

## Mobile Markdown dependency remediation — 13 September 2026

- Scoped the react-native-markdown-display dependency override to markdown-it 15.0.2, resolving linkify-it 6.1.0. This removes the old parser/linkifier versions implicated in the remaining high-severity advisories. Because the renderer declares markdown-it 10.x, this is an explicitly tested compatibility override, not a claim of upstream support for the combination.
- Added a browser-rendered mobile article regression through login, profile, blog list and article detail. It checks headings, emphasis, lists, quotes, code, table text and links, plus a script/unsafe-link fixture that must not create executable DOM markup. Existing billing/pricing regressions remain included. The test exercises the actual React Native Web renderer, with mocked article/API data.
- Verification: resolved npm tree shows markdown-it 15.0.2 and linkify-it 6.1.0; npm audit now reports 18 moderate package findings and no high/critical/low findings. Mobile typecheck/lint, Android/web JavaScript exports, all seven mobile Playwright checks and diff checks pass. The temporary Expo server was stopped. Native device rendering and remaining navigation/tooling dependency findings still need verification/remediation.
- This removes the two high-severity dependency findings; it does not establish complete application security or Ghana/store compliance. Remaining C01–C10 requirements and external release gates remain open.

## Xcode tooling dependency remediation and iOS project generation — 13 September 2026

- Scoped xcode's UUID dependency to 11.1.1, which retains a CommonJS entry point and `v4()` used by the installed xcode package. The resolved tree confirms the replacement. A direct interoperability check generated 1,000 distinct uppercase 24-character Xcode project identifiers successfully.
- Ran `CI=1 expo prebuild --platform ios --no-install` in an isolated temporary project copied from the current app/package configuration with shared installed dependencies/assets. Native iOS project generation and the current Expo plugins completed successfully, including the expo-iap Podfile configuration. No native directories were added to the repository, CocoaPods were not installed, and no app was compiled, signed or submitted.
- Mobile typecheck/lint and diff checks pass. npm audit now reports eight moderate package findings, all in the Expo Router/React Navigation/query-string/decode-uri-component chain; zero high/critical findings remain in this mobile dependency audit. This is dependency-scope evidence only.
- URI decoding remains open: [the advisory](https://github.com/advisories/GHSA-vcc3-ghjq-m6fr) identifies 0.5.0 as patched, but that release is ESM-only while installed query-string 7.1.3 expects a CommonJS callable export. A direct version override would not establish compatibility; a tested integration or upstream migration is required. No incompatible decoder override was applied.
- Native build/device/sandbox verification, Apple checkout/verification and all other remaining compliance gates remain open. iOS prebuild success does not establish store approval or legal compliance.

## Navigation URI-decoding remediation — 13 September 2026

- Replaced navigation's query-string 7 dependency with a local export-shape bridge around upstream query-string 9.5.1, which resolves patched decode-uri-component 0.5.0. The bridge exposes the named functions expected by Expo Router/React Navigation, supplies CommonJS/ESM entry points and declarations, and contains no copied parser implementation. Upstream code remains an explicit auditable dependency under the query-string-modern npm alias.
- The first file-override install omitted the upstream dependency even though audit reported zero findings; an actual import check caught the incomplete install. The final mobile manifest explicitly installs the upstream alias. A fresh isolated `npm ci --ignore-scripts` from the checked-in manifest/lockfile/vendor bridge then resolved and exercised navigation's query parser successfully. Zero findings were accepted only after the dependency tree included the actual patched parser and decoder.
- Added query compatibility checks for Unicode, repeated/null/empty parameters, URL round trips and bounded malformed decoding without prototype changes. All nine mobile Playwright checks pass, including actual login/profile/subscription/article navigation. Mobile typecheck/lint, Expo compatibility, Android/web exports, clean installation and diff checks pass; the temporary Expo server was stopped.
- Current mobile npm audit reports zero findings at every severity. This establishes resolution of the recorded dependency advisories, not comprehensive application security or compliance. The compatibility bridge requires Node >=20.19 and should be removed when the framework supports the patched upstream export shape directly. Device navigation, native billing/Apple implementation, broader Ghana compliance and external release evidence remain open.

## Apple signed transaction verification foundation — 13 September 2026

- Added Apple's official `@apple/app-store-server-library@3.1.0`. The adapter obtains transaction information from the configured App Store Server API, then independently verifies its signed JWS using server-owned Apple root certificates, online certificate checks, bundle ID, App Store app ID and explicit environment. Client input is limited to a numeric transaction ID and the expected account UUID supplied by the caller; receipt JSON is not accepted as evidence.
- Normalization requires an auto-renewable purchased subscription bound to the expected account, matching transaction/application/environment, valid chronology and a subscription group. It preserves original transaction, product, expiry, revocation and upgrade facts while stripping the account token and unrelated payload fields. It deliberately does not claim access eligibility before current subscription status/grace/retry and product mapping are implemented. Family-shared purchases are unsupported in this account-bound flow.
- Config and provider/signature failures are sanitized. Retryable certificate-verification failures remain retryable instead of being treated as a definitive invalid purchase. Sandbox is explicit and rejected in NODE_ENV=production; production never silently falls back to sandbox. Root/credential files and environment variables are documented in `.env.example`.
- Verification: all 754 API tests across 74 files pass with isolated Mongo enabled, including 23 new Apple normalization/provider-boundary cases. API typecheck/lint and diff checks pass. Apple API/signature classes are mocked in these tests; no real transaction, trusted certificate-chain/OCSP response, App Store Connect credentials or sandbox purchase was verified.
- Source: [Apple's server library documentation](https://github.com/apple/app-store-server-library-node) describes the server API credentials, Apple PKI root certificates and SignedDataVerifier usage. Current installed library signatures were checked during implementation.
- Required next: current subscription status and grace/retry handling, durable ownership/journal/mapping/activation, notifications and reconciliation, bounded transport/recovery behavior, native StoreKit checkout/restore, review-environment strategy and sandbox evidence. Backend dependency installation reports 19 audit findings (1 low, 4 moderate, 14 high) and a deprecated jsrsasign dependency; these require triage separately from the mobile zero-finding audit. No claim is made that the SDK introduced all reported findings. Overall C05 and the goal remain IN PROGRESS.


## Apple current subscription status verification — 13 September 2026

- Added current-chain verification using Apple's Get All Subscription Statuses API after verifying the submitted transaction's ownership. The response must match the configured bundle, App Store app ID and environment and contain exactly one matching original transaction chain. Both the latest transaction and renewal JWS pass through Apple's verifier; account, original transaction, subscription group, product and environment are checked across payloads.
- Access eligibility requires active status with a future transaction expiry, or billing grace status with a future signed grace expiry. Expired, billing-retry and revoked statuses deny access even if a historical transaction has a future expiry. Revoked/upgraded transactions override active status. Disabling auto-renew retains the current paid period; it does not immediately revoke access. This adapter only returns eligibility facts; it does not yet activate a stored entitlement.
- Verification: all 783 API tests in 74 files pass with isolated Mongo enabled, including 52 Apple cases covering status/expiry decisions, historical-to-current-chain lookup, mismatched and duplicate chains, account ownership, renewal signatures and sanitized provider failures. API typecheck/lint pass. Provider and signature classes remain mocked; live Apple trust-chain/OCSP and sandbox evidence are still outstanding.
- Sources: [Get All Subscription Statuses](https://developer.apple.com/documentation/appstoreserverapi/get-all-subscription-statuses), [Apple subscription status values](https://developer.apple.com/documentation/appstoreserverapi/status), and installed official server-library model declarations.
- Remaining C05 work: durable Apple ownership/journal, product mapping and activation, notifications/reconciliation, bounded provider transport, native checkout/restore, review-environment strategy and real sandbox evidence. Other C01–C10 obligations remain open; no release or comprehensive compliance claim.


## Apple durable purchase ownership and journal — 13 September 2026

- Added an Apple-specific journal keyed uniquely by application, environment and a hash of the original transaction ID. Renewals update the same immutable account owner; sandbox and production identities are separated. Latest transaction IDs are hashed, and the original transaction recovery identifier is encrypted using the existing AES-256-GCM vault with application/environment/chain/owner authenticated context. Normal journal queries exclude the encrypted field and do not return raw transaction identifiers or signed payloads.
- A verified transaction first resolves the chain identity. The journal revision is read before requesting current subscription status; updates compare that revision and the immutable owner. Concurrent first inserts and stale updates require fresh verification instead of replaying old provider facts. Provider eligibility is stored as an observation with entitlementState=pending; this does not activate application access.
- Current application/environment/original-chain/group identity must remain consistent across verification. The account binding is rechecked after provider calls so observed closure/suspension prevents persistence. Future activation must also recheck account eligibility. Failed provider reads preserve the previous observation.
- Verification: 793 API tests across 75 files pass with isolated Mongo enabled, including ten new real-Mongo Apple journal cases for encrypted recovery, renewal/revocation persistence, immutable ownership, simultaneous initial inserts and revision updates, closure/suspension during verification, identity changes and provider failure. API typecheck/lint and diff checks pass. Only Apple provider adapters are mocked in this integration suite; actual Mongo indexes, writes and revision conflicts execute.
- Remaining: Apple product mapping/preparation/activation and read-time expiry enforcement, recovery scheduling and notifications, native checkout/restore, bounded transport, retention classification and real Apple sandbox evidence. No public Apple completion route has been enabled before those prerequisites. C05 and the overall goal remain IN PROGRESS.


## Apple entitlement preparation, activation and access — 13 September 2026

- Connected the Apple journal to immutable Apple product mappings and captured entitlement snapshots. Products no longer offered for sale can still restore; missing/malformed mappings fail preparation. Feature keys and types are validated with the shared registry. Preparation and activation are separate, revision-fenced steps; a fresh provider observation clears both prepared revision and grant before any new access can be activated.
- Activation rechecks application/environment, account status/eligible role, provider state, revocation/upgrade flags and expiry. Read-time selection repeats provider eligibility, revision, product and expiry checks and bounds grants by current provider access expiry. Grace uses signed grace expiry. No scheduler is required to stop expired access. Active Apple candidates now participate alongside Google candidates in the existing effective-plan selection, preserving newer valid legacy plan precedence.
- Subscription responses identify Apple plans as app_store. Native subscription package filtering now excludes the free-plan switch for both Apple- and Google-managed paid subscriptions. Native Apple checkout itself remains unimplemented; no completion endpoint is enabled yet.
- Verification: 796 API tests in 75 files pass with isolated Mongo enabled. Apple real-Mongo integration coverage now includes actual entitlement resolution/quota features, inactive mapping restoration, preparation-before-activation, revocation, stale prepared revisions, account suspension, missing mappings, legacy precedence, read-time expiry, signed grace bounds and environment isolation. API/mobile typecheck and lint pass; diff checks pass. No additional browser/device or real Apple sandbox verification was performed in this checkpoint.
- Remaining C05 work: completion endpoint, durable retry/recovery, notifications/reconciliation, bounded provider transport, native StoreKit checkout/restore and live sandbox evidence. Retention/legal/store-release gates and other C01–C10 obligations remain open. Overall goal remains IN PROGRESS.


## Apple authenticated purchase completion and retry — 13 September 2026

- Added POST /api/store-billing/apple/complete for authenticated active landlords/property managers. The strict body accepts only a numeric StoreKit transaction ID; account and entitlement claims come from authenticated/provider/server state. The service performs fresh verified journaling, mapping preparation and revision-fenced activation in order and returns only journal/revision/provider-status/entitlement-state facts.
- Ownership/binding failures return 403, invalid/disallowed test purchases 422, concurrent revisions 409 with short Retry-After, and ambiguous provider/configuration/interruption failures 503 with retry instructions that discourage duplicate purchasing. No raw provider error or transaction identifier is forwarded into response/error logging. Processing responses use Cache-Control: no-store. Existing write limits and account/session middleware apply. OpenAPI documents the request, processing-versus-access distinction and retry behavior.
- A successful server response does not claim to finish a StoreKit transaction. The future native client must finish after confirmed processing; failed processing must remain retryable. Restoration repeats the same verified sequence and uses the existing original-chain journal.
- Verification: all 815 API tests across 77 files pass with isolated Mongo enabled. Thirteen HTTP endpoint cases exercise authentication, roles, deleted/suspended accounts, strict input, ownership, state responses and safe retry failures. Five completion-sequence cases cover interruption at each stage and re-verification. A real-Mongo scenario proves a mapping failure leaves a pending journal, then a repaired mapping/retry activates access in the same chain with no duplicate purchase record. API typecheck/lint and diff checks pass.
- Remaining: bounded provider transport, durable recovery/reconciliation, Apple notifications, native checkout/restore/transaction finishing and real sandbox evidence. C05, other C01–C10 obligations and external legal/store release gates remain open; the overall goal remains IN PROGRESS.


## Apple background purchase recovery — 13 September 2026

- Added a five-minute scheduler job for Apple pending/prepared purchases and active, grace-period or billing-retry subscriptions. Each bounded batch claims at most 20 rows by default with an expiring, uniquely identified lease. Application/environment filters prevent cross-app processing; sandbox recovery is disabled in production. Fully processed expired/revoked chains stop polling and will require notifications or an explicit restore to discover subsequent changes.
- Recovery decrypts the original transaction identifier using its application/environment/chain/owner context, checks its hash and invokes the full fresh-verification/completion sequence. Success schedules the next poll; failure records only a sanitized code with exponential backoff capped at one hour. Lease release and retry-state updates require the current lease ID, preventing an older worker from clearing a replacement worker's state. Scheduler errors do not log provider payloads.
- Verification: all 821 API tests across 78 files pass with isolated Mongo enabled. Five recovery integration cases exercise competing workers, crash-expired leases, backoff, identifier corruption, lifecycle-state selection, sandbox/batch constraints and lease fencing. An additional provider-adapter-mocked test executes the real journal, recovery, mapping, activation and entitlement lookup against MongoDB and proves a pending purchase becomes active without another client request.
- API typecheck/lint and diff checks pass. Provider/signature adapters remain mocked; no live Apple purchase was polled. Provider transport deadlines, real-time Apple notifications, native checkout/restore/finishing, live sandbox evidence and broader legal/store gates remain open. Batch bounds and lease expiry do not yet establish bounded provider-call duration. C05 and the overall goal remain IN PROGRESS.


## Apple provider transport deadlines — 13 September 2026

- Inspected the installed official Apple server SDK: its App Store API fetch had no timeout; OCSP certificate-status calls already use node-fetch timeouts. Added a subclass through the SDK's protected makeFetchRequest extension point, preserving its token signing, response validation and API error classification. Production/sandbox hosts are fixed by server environment.
- API requests now have a 15-second abort deadline spanning headers and full response-body consumption, reject redirects and enforce a 1 MB response-body limit. The bounded buffered response is returned to the SDK validator. node-fetch 2.7.0 is now an explicit pinned dependency instead of an implicit SDK transitive dependency; its resolved version was already 2.7.0.
- Signed-payload verification has a 35-second caller deadline classified as retryable provider_unavailable. Late verification results cannot resume journaling or activation. The official verifier exposes no cancellation hook, so its underlying OCSP work may continue until its own SDK transport timeout; this change bounds the caller without claiming immediate cancellation of certificate checks. API fetches are actively aborted.
- Verification: all 830 API tests in 80 files pass with isolated Mongo enabled. Real local HTTP socket tests cover stalled headers, stalled bodies, redirects, size limits and buffered error responses. Official SDK integration tests use a locally generated EC signing key with mocked HTTP transport to verify production/sandbox URLs, bearer signing, validation and error classification. Deadline tests confirm slow signature checks remain retryable. API typecheck/lint and diff checks pass.
- No live Apple credentials, certificates or purchases were exercised. The API dependency audit still reports 19 findings (1 low, 4 moderate, 14 high), tracked separately; no new security/compliance completeness claim is made. Apple notifications, native checkout/restore/finishing, sandbox evidence, remaining Ghana requirements and external legal/store release gates remain open. Overall goal remains IN PROGRESS.


## Apple signed server-notification verification — 13 September 2026

- Added strict signedPayload input and verification through the official SDK's verifyAndDecodeNotification with the existing certificate/caller deadlines. The envelope requires a v2 UUID/type/date and data matching the configured bundle, numeric app ID and environment. Future-dated envelopes beyond the existing clock allowance are rejected; delayed delivery remains valid for subsequent deduplication/current-state reconciliation.
- Non-test events also require independently verified nested transaction data. Transaction normalization enforces supported subscription/purchase structure, app/environment and chronology while dropping account tokens and unrelated payload fields. A signed TEST notification may omit its transaction. Summary, external-purchase and app-data payload families are rejected by this subscription adapter and will require separate handling if those capabilities are adopted.
- This authenticates event facts only. The verifier deliberately does not authorize an account or grant access; the consumer must resolve immutable journal ownership and freshly verify current subscription state. A nested appAccountToken is structurally validated here; comparison with the stored owner's binding belongs to the completion path.
- Verification: all 845 API tests across 80 files pass with isolated Mongo enabled, including 15 new envelope/nested-signature cases covering test events, app/environment identity, malformed payload families, delayed delivery, future dates, tampering, retryable verification failures and input bounds. Signature/provider methods remain mocked; no real Apple notification/certificate chain was exercised.
- API typecheck/lint and diff checks pass.
- Remaining: notification persistence/deduplication, HTTP delivery handling, owner lookup and current-state completion, refund/revocation outage behavior, native StoreKit checkout/restore/finishing and real sandbox evidence. Other Ghana/legal/store requirements remain open. Overall goal remains IN PROGRESS.


## Apple notification HTTP delivery and reconciliation — 13 September 2026

- Added POST /api/webhooks/apple, authenticated by the Apple signed payload rather than an app session. Supported subscription lifecycle events resolve the immutable journal owner by app/environment/original-chain hash, then execute fresh owner-bound purchase completion. Historical notification state never directly overwrites current provider state. Unknown owners remain retryable until device registration.
- Completed deliveries use the shared 30-day deduplication collection under an Apple/app/environment namespace. Acknowledgement follows successful reconciliation and durable dedup persistence; duplicates still undergo signature verification, then skip repeated completed work. Concurrent duplicate inserts are accepted only for unique-key conflicts; real database failures remain retryable. Signed TEST events are durably recorded without changing purchases.
- The route returns no-store 204 on success, 400 for invalid input/signature/app/environment, and retryable 503 for incomplete processing, unsupported consumption-event handling and unexpected failures. Provider payloads/errors are not passed to generic error logging. OpenAPI now documents App Store Connect endpoint configuration and delivery behavior.
- Verification: all 854 API tests across 81 files pass with isolated Mongo enabled. Eight HTTP cases cover signature authentication, immutable owner dispatch, dedup order, test events, missing owner, unsupported events, persistence races/failures and sanitized errors. A real-Mongo lifecycle case proves failed processing is not marked complete, revocation removes access after successful reconciliation, duplicate delivery does not repeat provider work, and later paid renewal restores actual plan entitlements.
- API typecheck/lint and diff checks pass.
- Remaining: durable signed refund/revocation overrides during provider outages, notification-history reconciliation for missed events, consumption-request workflow if enabled, native checkout/restore/finishing and live Apple sandbox/notification evidence. Current refund/revocation processing depends on a successful fresh provider read; immediate outage-safe removal is not yet claimed. Other legal/store gates and C01–C10 obligations remain open. Overall goal remains IN PROGRESS.


## Apple signed revocation overrides during provider outages — 13 September 2026

- Added a durable transaction-specific revocation collection keyed by application, environment and hashed transaction ID. Signed REFUND/REVOKE evidence is persisted before immutable-owner lookup or provider refresh, so it survives unknown-owner delivery and provider outages. REFUND_REVERSED records the opposite signed decision; ordinary purchase verification cannot erase either decision.
- Atomic conditional updates choose the newest signed event, not the latest delivery. Equal-time conflicts favor revocation; concurrent first inserts retry the same monotonic update against the unique-key winner. Duplicate/stale deliveries do not move the observation timestamp. No raw transaction IDs, account tokens or signed payloads are persisted in this collection.
- Preparation, activation and read-time access all consult the override for the purchase's current transaction. Revoked transactions remain unavailable despite an active-looking provider observation. A newer reversal requires provider verification strictly after local receipt of the reversal before it removes the restriction. Refunds for older transactions do not block a later renewal in the same subscription chain.
- Verification: all 855 API tests across 81 files pass with isolated Mongo enabled. The existing notification lifecycle test now proves access is removed immediately when fresh provider verification fails, before delivery acknowledgement. Additional real-Mongo checks cover concurrent refund/reversal inserts, signed-date ordering, equal-time conflicts, stale-event replay, reversal requiring fresh verification, persistent denial across ordinary refreshes and isolation from older refunded transactions. Provider/signature adapters remain mocked; no live Apple refund was issued.
- API typecheck/lint and diff checks pass.
- Remaining: missed-notification/history reconciliation, consumption-request handling if enabled, native StoreKit checkout/restore/finishing, retention classification and real sandbox/refund evidence. Other Ghana/legal/store requirements and external release gates remain open. Overall goal remains IN PROGRESS.


## Native iOS subscription checkout and restore implementation — 13 September 2026

- Added an iOS session-scoped App Store billing component using installed expo-iap 5.6.0, mounted alongside the existing platform-specific Google component. It initializes billing, obtains the server account binding, loads Apple-mapped StoreKit products, subscribes to purchase/error events, restores on startup/foreground without forcing store synchronization, and provides an explicit user-triggered restore that synchronizes with Apple. Connection initialization/teardown is serialized across session changes.
- Checkout supplies the server-issued appAccountToken and disables automatic transaction finishing. Completion sends only the StoreKit numeric transaction ID to the authenticated Apple endpoint. StoreKit finishTransaction(isConsumable:false) occurs only after active/revoked processing is confirmed; pending purchases, failed/incomplete server responses and changed accounts do not finish. A finishing failure can retry through fresh verification. No receipt/JWS or transaction identifier is persisted in app storage.
- Added iOS plan cards with StoreKit-localized recurring price/period, mapped benefits, restore/reload/manage actions and terms/privacy links. Unsupported subscription bundles and commitment pricing structures are excluded pending dedicated support. Introductory eligibility is not advertised as guaranteed; eligible terms remain confirmed in the Apple purchase sheet. Existing purchases route to restore/manage rather than starting another subscription. Subscription state refreshes after Apple completion.
- Verification: four focused Playwright-run pure contract tests pass for localized/exact product mapping, verification-before-finish ordering, pending/failed/incomplete/account-change guards and finish-failure recovery. Mobile typecheck/lint pass. Expo iOS export succeeds with 1,565 modules and produces a Hermes bundle in /tmp/rentos-apple-ios-export. This is a JavaScript export, not an Xcode build, signed app or simulator/device execution; the contract tests do not exercise the native StoreKit UI or full React session lifecycle. No additional API test run was needed because this checkpoint changes mobile/e2e code only.
- Remaining: native purchase-sheet/Ask-to-Buy/cancel/restore/account-switch testing, sandbox and production-review environment strategy, subscription changes/introductory-offer presentation, native build/signing/store metadata and App Store Connect products/credentials. Missed-notification reconciliation and other Ghana/legal/store requirements also remain open. Overall goal remains IN PROGRESS.


## Mobile account-switch request and refresh isolation — 13 September 2026

- Native readiness inspection confirmed Xcode is selected at /Applications/Xcode.app/Contents/Developer and simulator runtimes are installed; no simulator was booted. Before native purchase testing, inspection exposed a shared API-client race: an old request's 401 could refresh or retry using a replacement session, and a late refresh could overwrite current tokens.
- Added an in-memory session version advanced by login/logout/hydration, independent of token rotation. JSON requests and multipart uploads capture that version, reject responses/retries after a session change, and deduplicate refresh only within their originating session. A stale request cannot refresh/log out the replacement account, retry with its token or deliver its response body. Refresh and biometric-refresh token updates check the captured version. Hydration discards old stored state if a login/logout happened while storage was being read.
- Verification: all 18 mobile Playwright checks pass, including five new deterministic concurrency cases for stale 401s, account changes during refresh, same-session refresh sharing, independent new-session refresh and late response bodies. Existing Expo-web login/subscription/payment/article/navigation regressions also pass. Mobile typecheck/lint and diff checks pass. Temporary Expo web server stopped after verification.
- Limits: this verifies request/session isolation and browser regressions, not native StoreKit or biometric hardware behavior. SecureStore write ordering across independent biometric enrollment/login/logout operations still needs explicit native lifecycle review. No iOS app was built or launched in this checkpoint. Native purchase-sheet/session testing, real sandbox evidence, missed notifications and remaining Ghana/legal/store requirements remain open; overall goal remains IN PROGRESS.


## Mobile credential persistence ordering — 13 September 2026

- Routed auth-session saves/deletes/hydration reads and biometric credential operations through one serial storage queue. A later logout deletion or login save cannot be overtaken by an earlier slow write, and reads wait for prior operations. Failed writes reject their caller without blocking later cleanup.
- Biometric enrollment and disable use a separate operation generation. Disable invalidates earlier enrollment/rotation immediately before waiting for server revocation. Saved credentials check both operation generation and auth-session identity before and after persistence; superseded/partial writes attempt cleanup while still holding the queue, before any newer credential write runs. Enrollment/rotation errors propagate instead of reporting a successful update after persistence failed.
- Biometric disable attempts both local key deletions and reports storage failure; server revoke-all remains best effort as before. It no longer waits for the network before invalidating old local work. Queuing does not claim to make OS storage succeed when the keychain is unavailable; native failure handling still requires device evidence.
- Verification: all 23 mobile Playwright checks pass, including five new storage-concurrency tests with an injected asynchronous storage adapter and the existing Expo-web login/subscription/payment/article/navigation regressions. Cases cover delayed old writes versus logout/new login, reads after writes, continued cleanup after failure, disabled late enrollment, account changes during persistence, and failure propagation after attempting both biometric key deletions. Temporary Expo web server stopped afterward.
- Mobile typecheck/lint and diff checks pass.
- No native keychain/biometric hardware, signed iOS build, StoreKit sheet or sandbox purchase was exercised. Device lifecycle/persistence-failure UX, native billing verification, missed notifications and remaining Ghana/legal/store requirements remain open. Overall goal remains IN PROGRESS.


## Biometric disable confirmation and recovery UX — 13 September 2026

- Corrected settings' unconditional claim that all biometric sessions were revoked. The disable operation now reports confirmed server revocation separately from successful local cleanup. Settings show persistent status/error text and a retry action when the server could not confirm revocation; local storage failures remain errors. Device management remains reachable even when this device has biometrics disabled or unavailable.
- Removed the redundant /auth/login request during biometric enrollment. The enrollment endpoint already requires password reauthentication, so the extra login created an unused session without adding an authorization check. Enrollment now performs the biometric prompt and password-protected enrollment directly. Success copy no longer exposes internal token implementation details.
- Account closure preserves its completed state and uses accurate cleanup-failure wording if biometric credential removal subsequently fails, rather than implying the account deletion failed.
- Verification: six credential-operation tests pass, including local-success/remote-failure distinction and storage failure propagation. A focused Expo-web browser test verifies Profile → Settings → Security → biometric device management while local biometrics are unavailable. Its initial hard-navigation test incorrectly discarded the in-memory web session; the corrected test follows the actual in-app route and passes. Mobile typecheck/lint and diff checks pass. Temporary Expo server stopped.
- Native biometric prompts, offline/revocation retry UI on device, keychain failure presentation and StoreKit sandbox flows remain unverified. Other Ghana/legal/store requirements and external release evidence remain open; overall goal remains IN PROGRESS.


## Native iOS build preparation and permission minimization — 13 September 2026

- Generated the iOS project in an isolated copy at /var/folders/26/rk022z3s53q7fbbwxj0mh33m0000gn/T/rentos-native-ios-t5velugn, with current mobile sources/configuration and linked installed dependencies. Xcode 26.6 is available. CocoaPods installation completed successfully with 103 Podfile dependencies and 110 installed pods, including OpenIAP. No native directories were added to the shared repository.
- Native Info.plist inspection exposed unused camera/microphone permission prompts and background audio. Source inspection found only chat notification playback and image-library selection for property photos. Updated app.json: expo-audio disables microphone recording and background recording/playback; expo-image-picker disables camera/microphone and explains property-photo selection. Expo introspection confirms no iOS camera/microphone usage descriptions or background-audio mode, and Android manifest removal directives for CAMERA/RECORD_AUDIO. The temporary native project was regenerated with the revised configuration; its old generated audio mode was removed before regeneration, and direct Info.plist checks passed.
- CocoaPods generated PrivacyInfo.xcprivacy with dependency required-reason API declarations but an empty collected-data list. This is not evidence that RentOS collects no data; app-level data-flow/privacy-manifest and store disclosure reconciliation remains required.
- Native simulator build is RUNNING, not yet passed: exec session 34704. Command: xcodebuild -workspace RentOSGhana.xcworkspace -scheme RentOSGhana -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/rentos-ios-derived CODE_SIGNING_ALLOWED=NO build. Workspace directory: the temporary copy's ios folder. Output: /tmp/rentos-ios-build.log. Last inspected output shows progressing native compilation for arm64/x86_64 and no error/terminal marker. Poll this existing session; do not restart merely because observation is slow.
- Supporting logs: /tmp/rentos-native-pods.log, /tmp/rentos-native-prebuild.log and /tmp/rentos-expo-native-config.json. /tmp/rentos-native-build-path records the temporary root. Repository diff checks pass. No simulator launch, device/sandbox purchase, signing, submission or full compliance claim. Overall goal remains IN PROGRESS.


## iOS simulator native build and first launch — 13 September 2026

- The original simulator native build (exec 34704) completed successfully. /tmp/rentos-ios-build.log ends with BUILD SUCCEEDED. The generated app is /tmp/rentos-ios-derived/Build/Products/Debug-iphonesimulator/RentOSGhana.app; native OpenIAP, Expo and React Native compilation/linking completed. Existing dependency warnings include an old SDWebImage resource-bundle deployment target and always-running script phases; no compiler failure occurred.
- Created and booted a dedicated iPhone 17 simulator running iOS 26.5: RentOS Compliance iPhone, CF9ACF95-0176-41C3-9038-7B46CB5CE620. Installed and launched gh.rentos.mobile. Initial Metro from the isolated project could not resolve the linked local query-string bridge; stopped that server and served the real mobile workspace instead. Current workspace Metro bundled 1,729 iOS modules successfully and the native login screen rendered, visually inspected in /tmp/rentos-ios-launch-current.png.
- The unsigned simulator build reports expo-notifications keychain access failure due to a missing entitlement. This prevents a clean credential-runtime acceptance result despite successful build/rendering. A follow-up simulator ad-hoc signing build is RUNNING in exec session 48909, using the same workspace/derived data and CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=-. Log: /tmp/rentos-ios-signed-build.log. Poll this existing handle before further build action; then inspect embedded entitlements, reinstall and retest keychain startup. No production signing identity or provisioning change was made.
- Current workspace Metro remains RUNNING in exec session 26789 on localhost:8581 with EXPO_PUBLIC_API_URL=http://localhost:3502. The simulator's app-domain RCT_jsLocation is localhost:8581. The API is not started for this unauthenticated launch check. Preserve these handles for the next native verification step; the temporary project root remains in /tmp/rentos-native-build-path.
- Reviewed [Apple's data-use manifest guidance](https://developer.apple.com/documentation/bundleresources/describing-data-use-in-privacy-manifests): app data categories, identity linkage, tracking and purposes require explicit declarations, with SDK manifests aggregated separately. Source inspection confirms account contact details, optional Ghana Card ID, property photos/locations, tenant-profile updates, chat content and push registration flows; the generated empty collected-data list is not a completed app-data review. Manifest/disclosure reconciliation remains open.
- No authenticated native purchase, biometric prompt, StoreKit sandbox payment, release archive or store approval was verified. Overall goal remains IN PROGRESS.


## Signed simulator storage and unfinished Apple transactions — 13 September 2026

- The existing ad-hoc simulator build (exec 48909) completed successfully; /tmp/rentos-ios-signed-build.log ends with BUILD SUCCEEDED. Reinstalled and launched the signed app on the dedicated simulator. Native login renders without the earlier expo-notifications keychain warning; screenshot /tmp/rentos-ios-signed-launch.png was visually inspected. Simulator generated entitlements contain FAKETEAMID.gh.rentos.mobile; these are development simulator artifacts, not production provisioning evidence.
- Connected to the app's local Hermes inspector and exercised the native ExpoSecureStore module with one dedicated temporary probe key: write, matching read, deletion and null read all passed. No user credentials were read or modified. This verifies basic simulator keychain operations, not biometric access control, app-auth persistence across restarts, physical-device security or production signing.
- Found that expo-iap's default available-purchase list covers active purchases and may omit expired transactions still awaiting completion. Added a shared restoration inventory that combines available purchases with getPendingTransactionsIOS and deduplicates Apple transaction IDs. Both restoration and pre-checkout duplicate protection use it. Failure to read either list rejects the inventory rather than permitting checkout using an incomplete result. Pending payments still cannot be verified/finished as completed purchases.
- All six Apple pricing/completion/restoration contract tests pass, including expired unfinished transactions, duplicate inventory entries, pending-payment protection and inventory failure. Mobile typecheck/lint pass. All 27 mobile regression checks pass (38.1s), including five rendered Expo-web flows and the pricing, restoration, request/session and credential concurrency contracts. Log: /tmp/rentos-mobile-full-latest.log. Native StoreKit SDK inventory and checkout still require sandbox/device evidence.
- Workspace Metro remains in exec 26789 on port 8581; the dedicated simulator is booted. The original and signing builds are both complete. No API is running on 3502 and no authenticated native billing flow was claimed. Privacy declarations, missed store notifications and remaining Ghana/legal/store obligations remain open. Overall goal remains IN PROGRESS.


## App privacy manifest and store data inventory — 13 September 2026

- Reconciled native form submissions, API calls and selected server models/reporting against current Apple category/purpose definitions and Google Play Data safety guidance. Added `docs/compliance/mobile-data-disclosures.md` with source-linked flows, store mappings and explicit unresolved release decisions. This inventory includes financial, message, AI-content, device, search and profile data rather than just registration fields. It is not a submitted store form or a comprehensive provider/retention assessment.
- Added 17 confirmed app-level collected-data categories to `expo.ios.privacyManifests` in `apps/mobile/app.json`, with account linkage, functionality purposes and reporting/personalization purposes where identified. No app-level cross-company tracking integration was found; tracking is false with no tracking domains. Advertising/SDK/provider and final binary reconciliation remain release requirements.
- Verification: Expo introspection passes; every configured category and purpose matches the current values in Apple's official documentation JSON. Isolated iOS prebuild passes. Parsed the generated `ios/RentOSGhana/PrivacyInfo.xcprivacy` and verified exact equality with all 17 configured entries, tracking false, and preservation of four existing dependency required-reason API categories. Log: /tmp/rentos-privacy-prebuild.log. Git diff checks pass. No application TypeScript changed, so the prior 27 mobile regression results were not rerun for this configuration/documentation slice.
- The existing installed simulator binary predates this manifest change. A final archive/build and aggregated privacy report are still required; prebuild does not prove store acceptance. Cloud/provider arrangements, contextual sponsorship measurement, sharing classifications, optional/required collection, sensitive/diagnostic data, AI consent, deployed policies and statutory retention remain open. C02/C06 and the overall goal remain IN PROGRESS.


## Explicit permission for AI writing and legal chat — 13 September 2026

- Source inspection found that /ai/chat, /generate, /listing, /formalize, /translate and /case-summary sent submitted content to Anthropic without an explicit sharing prompt. Legal chat also calls OpenAI embeddings for its retrieval query when configured. [Apple App Review Guideline 5.1.2(i)](https://developer.apple.com/app-store/review/guidelines/) requires disclosure and explicit permission before third-party AI sharing.
- Added per-request web/native confirmation naming Anthropic and OpenAI, describing text/details and chat-history scope, and allowing cancellation before any AI request is sent. Consent is not remembered as an account-wide grant. The API clients snapshot the body before asking and reject a changed session before sending. Native uses an Allow and send / Cancel alert; Expo-web and web use a browser confirmation.
- All six external routes require the current aiSharingConsent value before executing their handlers. Missing, outdated or non-string permission is rejected with 403. This is the first-party client's request acknowledgement, not independently verifiable proof of a human click or permission for another person's data. Existing authentication/role/rate checks remain in place. Local listing-quality scoring does not require provider permission.
- Legal chat restores the question and previous conversation after cancellation; no artificial AI error reply is added. Added an accessible send-button label. Removed the raw question excerpt from RAG debug logging. OpenAPI documents the request field and failure response.
- Initial verification: 11 HTTP tests cover all six protected routes, invalid/stale acknowledgement and accepted chat execution without passing the consent field to the provider. A real Expo-web browser test passes: cancel sends nothing and preserves the question; allow sends the versioned acknowledgement; the next question asks again and can be cancelled. No real AI provider was contacted. Full API suite passes: 866 tests across 82 files with isolated Mongo enabled (/tmp/rentos-api-ai-consent-full.log). All 28 mobile regression checks pass (/tmp/rentos-mobile-ai-full.log). API/web/mobile typechecks and lint pass, as does git diff --check.
- Remaining: native Alert/device and standalone web flow evidence, provider retention/contract/country disclosure, prompt-error/log sanitization elsewhere, appropriate authority for case-summary third-party data, and background/indirect embedding flows (for example property indexing and semantic searches) outside these six routes. Public abuse-check classifier transfer requires separate evaluation. Consent on these routes does not establish complete AI/Ghana privacy compliance. Overall goal remains IN PROGRESS.


## Semantic-search permission and cache privacy — 13 September 2026

- Traced indirect embedding callers: property create/update, recommendations (profile preferences and missing property embeddings), semantic search, legal-document editing/seeding and maintenance backfills. These are distinct from the six AI-writing/chat routes. This checkpoint addresses the direct semantic-search endpoint; the other transfers remain open.
- POST /properties/search/semantic now requires current per-request AI sharing acknowledgement before cache lookup, input processing or OpenAI access. Web/native API clients cover the endpoint and show an OpenAI-only search disclosure. Existing writing prompts now name only Anthropic; legal chat names Anthropic and OpenAI with conversation/query scope. Normal keyword search is unchanged. No dedicated semantic-search UI caller was found in the current web/mobile source; API-client coverage does not establish a completed user-facing semantic-search feature.
- Semantic queries are trimmed and bounded to 4,000 characters. Cache keys hash a structured tuple of query/filters/limit using SHA-256 instead of embedding raw query text; this also prevents delimiter ambiguity between filters. Cache values still include the response query for five minutes, so hashing is not encryption or complete query erasure. It prevents raw queries from appearing in cache-key diagnostics.
- Fixed OpenAPI's incorrect /properties/semantic-search URL to /properties/search/semantic and documented consent, limits, filters and response fields.
- Fourteen focused HTTP checks pass across semantic search and existing AI consent. New cases prove permission rejection before cache/provider calls (including when a cached result exists), blank/oversized input rejection, opaque filter-specific keys and provider input containing only the search text. The existing real Expo-web permission test still passes after provider-specific prompt changes. No external AI service was called. All 869 API tests across 83 files pass with isolated Mongo enabled (/tmp/rentos-semantic-full-api.log). API/web/mobile typecheck and lint pass; API typecheck was repeated after the OpenAPI correction. Git diff checks pass.
- Background property indexing, profile-driven recommendation transfers, legal-document ingestion, public classifier hosting/data flow, provider contracts/retention and full Ghana/store privacy requirements remain unfinished. Overall goal remains IN PROGRESS.


## AI provider failure privacy and batch alignment — 13 September 2026

- Anthropic generation, listing, formalization, translation, chat and case-summary failures no longer log provider error messages. Legal retrieval failure logs also omit the underlying error. OpenAI single/batch embedding fallbacks use fixed log text. Errors returned from generation helpers no longer retain raw provider errors in nested causes, which could expose submitted data through later logging. Narrow preserve-caught-error lint exceptions explain this deliberate privacy boundary; the global rule remains enabled. Configuration failures use user-facing availability text instead of environment-variable instructions.
- Corrected embedding batch alignment: blank inputs are removed before provider submission, so response indexes refer to the compacted batch. Responses now map through that compacted batch's original indexes before building output. Leading/middle/trailing blanks retain fallback slots; reordered provider results map to the right nonblank input. Previously a later property's vector could be assigned to a blank/earlier record.
- Eight focused tests pass for failure-message/cause redaction across four generation operations, chat/retrieval logs, embedding fallback logs, batch order/blank alignment and all-blank batches avoiding provider calls. SDK adapters are mocked; no user input was sent to a real provider. All 877 API tests across 84 files pass with isolated Mongo enabled (/tmp/rentos-ai-provider-privacy-full.log). API typecheck passes. API lint and git diff checks pass. No frontend changes required a new browser run for this slice.
- Still open: end-to-end AI transport deadlines/body limits (SDK timeout configuration alone has not been accepted as evidence), indirect/background transfer consent, provider terms/retention, other caller logs, legal corpus accuracy and the remaining Ghana/store requirements. No comprehensive AI compliance claim. Overall goal remains IN PROGRESS.


## Bounded AI provider transport — 13 September 2026

- Added a shared buffered JSON fetch adapter for the installed Anthropic/OpenAI SDKs. A 30-second deadline remains active through header arrival and complete response-body consumption; oversized decompressed bodies abort above 16 MiB. Caller cancellation is combined with the transport signal. Redirects and endpoints outside each configured official provider origin are rejected before transfer. SDKs retain request authentication and response/error parsing.
- SDK automatic retries are disabled, avoiding silent duplicate submissions and multiplied timeout windows. Their separate 35-second timeout is a secondary bound; the transport body deadline is independently tested. Fixed origins mean custom base-URL proxies are unsupported; documented this with the limits in apps/api/.env.example. The adapter is for existing non-streaming calls, not future streaming features.
- Fifteen focused tests pass across transport and provider privacy. Seven transport cases use an actual local HTTP server to verify stalled headers, stalled body, oversized response rejection, disallowed origins, redirect refusal, preserved HTTP error status/body/request ID, caller cancellation, and successful parsing by both real installed SDKs (some cases cover multiple assertions). No external provider credentials or requests were used.
- API typecheck/lint and diff checks pass. All 884 API tests across 85 files pass with isolated Mongo enabled (/tmp/rentos-ai-transport-full.log). This establishes a per-provider-call network/body bound, not a total deadline covering sequential retrieval + generation, database work or every other remote service. Background AI transfer permission and provider operational/legal obligations remain open. Overall goal remains IN PROGRESS.


## Short-tenancy advance rule and verified statutory copy — 13 September 2026

- Located Parliament's newer repository bitstream and downloaded the Act 220 PDF successfully with curl after web fetches and the older repository URL returned 403. Source: https://repository.parliament.gh/server/api/core/bitstreams/1e10f889-0170-4f31-8f43-a6d2e8d6e536/content . Local evidence: /tmp/rentos-rent-act-220.pdf, SHA-256 e27ccfe0562f2b7409f630eed03d6975eafb3f17c7f844603347576778b789f7. Extracted text and visually inspected page 16, containing s.25(5). This is an archived statute copy with amendment annotations, not proof of a complete current amendment/commencement audit. The separate BCP download timed out.
- Section 25(5) distinguishes monthly-or-shorter tenancies (one month's advance) from terms exceeding six months (six months' advance). Added a one-month advance violation for dated agreements ending within one calendar month of commencement. The boundary uses a month-end-clamped UTC anniversary, covering February, leap years and year changes rather than assuming 30 days. Existing shared model flag generation and signing revalidation apply the rule to new and legacy agreements.
- Nineteen agreement tests pass, including seven new short-term/date-boundary/model/signing cases. A two-month advance on a one-month legacy agreement cannot be signed even if stored flags are empty. One month is accepted by this limited advance check; that is not certification of the entire agreement.
- Corrected RENT_LAW.receiptCitation from section 23 to section 33 after reading the receipt provision on page 18. The receipt audit must check premises identification, furnished status, amount, covered period and tenant name, and assess stamping where applicable. Generating any payment PDF alone does not establish that coverage.
- Remaining classification gaps: periodic monthly tenancies cannot reliably be identified from a long start/end date range; the model needs an explicit tenancy basis. Terms between one and six months, listing/application/renewal/payment boundaries and natural-language abuse-checker claims still need review. The existing six-month platform guard is not an authoritative classification of those cases. Rent cards under section 20, receipts, other statutory content, amendment currency and legal review remain open. All 891 API tests across 85 files pass with isolated Mongo enabled (/tmp/rentos-short-tenancy-full.log); API typecheck/lint and diff checks pass. Overall C01 and goal remain IN PROGRESS.


## Receipt issuance gap and legal corpus correction — 13 September 2026

- Payment audit found only an optional receiptUrl field and a web link shown when it exists. No rent-receipt issuance endpoint/generator was found, and Payment has no rent-period allocation or immutable tenancy receipt snapshot. Provider email confirmations cannot establish section 33 particulars. Required engineering remains: explicit covered-period allocation, accurate payer/tenant/premises/furnished-state snapshots, completion-only issuance, historical immutability, private access, web/native retrieval, refund/correction handling and retention review. Do not infer a payment's covered period merely from paidAt or its amount.
- The bootstrap receipt document still cited section 23 and asserted a 100-penalty-unit sanction. Replaced it with a source-checked section 33 paraphrase covering the statutory particulars and conditional stamping; removed unsupported numerical penalty/evidentiary claims. Public keyword receipt guidance now uses the shared corrected citation and does not state the unverified numeric penalty. The rest of the corpus remains unaudited.
- Added exact-content correction for persisted legacy copies, matching old title/source/section/content. Corrected records retain activation state; administrator-edited content is preserved for review, and old embeddings are cleared rather than reused as if they described the corrected source. Bootstrap invokes this correction before seeding, and a dedicated `apps/api/src/scripts/correctReceiptCorpus.ts` applies only the correction without AI calls or other seeding. It requires explicit MONGO_URI and does not depend on unrelated JWT configuration.
- Real-Mongo verification proves exact-copy replacement, stale-vector clearing, reviewer-edit preservation, inactive-state preservation and idempotent rerun. All 892 API tests across 86 files pass (/tmp/rentos-receipt-corpus-full.log). The dedicated CLI ran successfully against the isolated local database and reported zero remaining matching records; no production database was changed. Initial CLI startup exposed unnecessary application-config/JWT coupling, which was removed before the successful run.
- Production correction deployment, edited legacy-document review/re-embedding, full receipt implementation and broader Ghana/store requirements remain open. API typecheck/lint and git diff checks pass. Overall goal remains IN PROGRESS.


## Explicit rent-payment period capture — 13 September 2026

- New rent-payment POST requests require an explicit rentPeriod with inclusive startDate/endDate strings. Web date fields and native date-entry fields ask what period the payment is towards; no default allocation is inferred from paidAt or amount. The UI explicitly avoids claiming the entire period is fully paid. Web date bounds use the selected agreement.
- The API rejects missing, impossible, reversed and out-of-agreement dates before payment creation/provider collection. Period data is written with the pending payment before the provider call. Payment schema retains optionality for historical/non-rent records, which remain unallocated rather than receiving invented dates. New/old clients must send the updated contract for new rent payments; OpenAPI and payment fixtures were updated.
- Idempotency checks now compare method and period in addition to agreement/amount, including the duplicate-key race path. This prevents reusing a key for another allocation/method. Complete phone/payload fingerprint review and interrupted provider-initiation recovery remain open.
- Nine focused tests pass for date validity, inclusive one-day periods, agreement bounds, required input, persistence-before-provider ordering, changed-period replay and actual Mongo immutability. The first persistence test exposed that immutable on a parent subdocument did not stop nested-path edits; field-level immutable constraints closed that gap, and the rerun passes. These are Mongoose write-path guards, not protection against direct database-administrator writes.
- Two Expo-web payment checks pass, including explicit verification of the submitted rentPeriod and preserved bank-transfer instructions. The web payment and suspended-obligation fixtures were adjusted but have not yet been rerun against a live API in this checkpoint. All 901 API tests across 88 files pass with isolated Mongo enabled (/tmp/rentos-period-final-api.log). API/web/mobile typechecks and lint pass; changed API/web files were rechecked after follow-up corrections. Git diff checks pass.
- Still required for statutory receipts: immutable tenant/premises/furnished-state receipt snapshot, confirmed-payment issuance, historical receipt access, private download, corrections/refunds and period allocation of historical payments through an authorized workflow. Capturing a payer-selected period is not itself a landlord-issued receipt or proof of full payment. Overall C01 and goal remain IN PROGRESS.


## Rent-payment receipt context snapshot — 13 September 2026

- New rent-payment initiation reads only tenant/landlord names and property title/address/furnished status from server-owned records, captures a versioned timestamped snapshot, and writes it with the payment before requesting provider collection. No identity numbers, phone numbers, emails or full profiles are copied into this snapshot. Client-supplied receiptContext is ignored.
- The snapshot records the selected tenancy details at payment initiation; it is not itself a receipt, paid-status confirmation, verified identity assertion or atomic snapshot of multiple source documents. Whole-context and nested fields are immutable through ordinary Mongoose update paths. Later profile/listing changes do not change the payment's saved details. Missing/deleted parties, missing/incomplete premises or unresolved legacy IDs leave context absent instead of inventing particulars or preventing an otherwise authorized rent payment. Database failures still propagate before provider initiation.
- Three new real-Mongo cases cover complete capture (including furnished:false), preservation across source edits and attempted direct/nested snapshot updates, missing parties/property/legacy IDs, and incomplete addresses. The existing controller test proves capture occurs before collection and a forged client snapshot is not persisted. Eleven focused context/period tests pass.
- All 904 API tests across 89 files pass (/tmp/rentos-receipt-context-full.log). The final unresolved-ID and forged-input guards were then verified by the focused rerun. API typecheck/lint and git diff checks pass. No web/native UI was changed in this checkpoint.
- Receipt issuance, completed-payment snapshots of amount/reference/paid date, protected download, historical allocation/context resolution, correction/refund lifecycle and statutory retention remain open. Incomplete historical records must not be presented as complete statutory receipts. Production migration/operational review and the wider Ghana/store obligations remain unfinished. Overall C01 and goal remain IN PROGRESS.


## Durable confirmed-rent receipt issuance API — 13 September 2026

- Added POST /payments/:id/receipt for the payment's tenant or landlord. Session authentication applies; receipt-specific ownership is enforced inside the service and does not grant administrator/delegated issuance rights. Suspended users retain this owned-obligation action. Responses are no-store; client-supplied amounts/ownership/receipt fields are ignored. Existing authorized general payment views retain their established access rules.
- First issuance requires a completed rent payment, valid saved rent period, complete versioned receipt context, recorded paid date, positive finite amount and reference. It does not fetch replacement names or addresses from current profiles. Pending/processing/failed/refunded payments cannot acquire a new receipt, and incomplete legacy records return a resolvable conflict rather than an invented document.
- The issued record is embedded on the payment and created with a conditional write matching status, original financial fields and saved period/context. Concurrent tenant/landlord requests converge on one receipt number and original snapshot. Normal field updates cannot rewrite issued fields. Later retrieval returns that original receipt alongside current paymentStatus, including refunded status; it does not claim the whole period is fully settled.
- Fourteen focused tests pass: nine real-Mongo issuance cases plus five HTTP boundary checks. Evidence includes eight concurrent requests from both parties returning one durable receipt, original amount/number surviving later source updates/refund, nested receipt modification rejection, non-rent/outsider/unconfirmed/incomplete denial, session-derived identity, no-store responses and suspended access. No external payment or production database was involved.
- OpenAPI documents the endpoint and immutable receipt/current-status distinction. All 918 API tests across 91 files pass with isolated Mongo enabled (/tmp/rentos-receipt-issuance-full.log); API typecheck/lint and diff checks pass. This implements the durable record/API, not a rendered statutory receipt, automatic issuance at settlement, delivery, stamping or store/legal approval. Next steps include printable/private download and web/native controls, refund/correction presentation, historical resolution and retention classification. Overall C01 and goal remain IN PROGRESS.
## Private printable receipt checkpoint — 13 September 2026

- Added GET /payments/:id/receipt.html for authenticated payment tenants/landlords, including suspended parties. It only reads an existing rent receipt; missing issuance returns 409 and outsiders receive 404. No URL bearer token or public storage link is introduced. OpenAPI documents the contract.
- HTML uses immutable issued particulars, escaped text, no external assets/scripts, restrictive CSP, no-referrer, nosniff and no-store. Copies retain original receipt amounts after refunds while showing current payment status and the copy-generation timestamp. The covered period is explicitly inclusive; the copy does not claim the outstanding tenancy balance is settled. Stamping remains an external requirement where applicable.
- Verification: 919 API tests across 91 files passed with isolated Mongo enabled (/tmp/rentos-receipt-api-full.log); after adding two more access cases, all eight endpoint tests passed. API typecheck/lint and diff checks pass. Three Chromium rendering checks cover Unicode/hostile text, narrow/desktop layout, print media and refund status (/tmp/rentos-receipt-render.log). Visually inspected /tmp/rentos-receipt-390.png and /tmp/rentos-receipt-1000.png. These are HTML checks, not generated-PDF or complete user-flow evidence.
- Remaining: authenticated web/native receipt controls, automatic settlement issuance and delivery, historical missing-data resolution, corrections/retention/stamping workflow and the wider Ghana/store requirements. Overall C01 and the goal remain IN PROGRESS.
## Web receipt access checkpoint — 13 September 2026

- Payment details now show an owned completed/refunded payment's receipt action. Retrieval issues/retrieves the durable original and then fetches its protected HTML with Authorization headers. Tokens never enter URLs. Each operation has a 20-second deadline; session changes are checked after reading each response, and closing/unmounting the control aborts pending work. An expired session is reported without silently switching the request to another account.
- The app displays a sandboxed iframe without script permission, alongside refresh and browser print/save-PDF controls. Receipt errors are visible and retryable. This replaces the old optional public receiptUrl link in the web payment dialog. Native access remains unimplemented.
- Evidence: live local browser/API/database simulated rent collection followed by receipt issuance, preview, forced 409/retry, refresh and print-function invocation passed (/tmp/rentos-web-receipt-flow.log). The test now asserts the newly created payment rather than an arbitrary first row. A separate delayed-response test confirmed an account change rejects the earlier session's receipt (/tmp/rentos-web-receipt-session.log). Web typecheck/lint and diff checks pass. Preview visually inspected at /tmp/rentos-web-receipt-preview.png; print action moved above the preview for easier access. The OS print dialog and saved PDF have not been verified.
- Remaining: native receipt controls, automatic settlement issuance/delivery, legacy missing details, corrections/stamping/retention and the rest of C01–C10. No production release or overall compliance claim.

## Mobile receipt and payment history checkpoint — 13 September 2026

- Added receipt controls for owned completed/refunded rent payments, available to tenant or landlord. Opening retrieves the durable receipt through the authenticated API; a selectable native text view preserves receipt number, parties, premises, furnished state, amount, covered dates, payment/issuance times, current status and the copy's status-check time. Refunds retain the original amount and identify the refund. The share button opens the platform share chooser with the full text and explicitly identifies the personal details included; no automatic delivery occurs.
- API POST now accepts an optional AbortSignal. Receipt closure/session changes abort pending work; existing session-version checks reject responses belonging to another session. The receipt UI times out after 20 seconds, exposes failures and allows retry/refresh. This UI deadline does not establish a deadline for a separately running shared token-refresh operation.
- Fixed mobile payment history stopping at the first 20 records. Page controls expose older payments/receipts, server aggregates replace page-only totals, and missing totals display as unavailable. Request ordering prevents an older page response overwriting a newer selection. Failed loads show a retry for the failed page instead of claiming there are no payments; the payment action is positioned above pagination.
- Evidence: all 31 mobile checks passed (/tmp/rentos-mobile-receipt-full.log). After autoload, retry-page and control-placement refinement, the three affected checks passed at desktop and 390px widths (/tmp/rentos-mobile-receipt-final.log and /tmp/rentos-mobile-receipt-narrow.log). These use actual Expo components with mocked APIs: both parties, unauthorized-control absence, 409/retry, refund particulars, refresh, close, older-page retrieval, full-set transaction totals and history error recovery. Narrow receipt preview visually inspected at /tmp/rentos-mobile-receipt-tenant.png. Mobile typecheck/lint and diff checks pass.
- Remaining: device receipt/share-sheet verification, native PDF/printing, automatic settlement issuance and delivery, legacy missing-data resolution, corrections/stamping/retention, and other C01–C10 requirements. This checkpoint does not establish statutory delivery or full Ghana/store compliance.

## Automatic receipt issuance and recovery checkpoint — 13 September 2026

- Shared provider finalization now attempts receipt issuance after a confirmed rent transition. Issuance failures do not undo settlement or skip subsequent existing payment handling. A five-minute scheduled job selects up to 50 eligible records (hard cap 100), claims each with a UUID and one-minute expiry, and uses the same conditional immutable issuance service as owner requests. Claim-fenced updates prevent a stale worker releasing another worker's claim. An interrupted process leaves the payment eligible after expiry.
- Retry state records attempt count, next attempt time and coarse details_or_state/temporary_failure reasons; raw errors and personal particulars are not stored in that state. Incomplete records wait 24 hours for repair; temporary failures use capped exponential delay. Non-rent and non-completed records cannot receive a new receipt. Concurrent manual/background issuance converges on the original receipt.
- Verification: all 928 API tests across 91 files pass with isolated Mongo enabled (/tmp/rentos-auto-receipt-api.log), including 16 receipt cases covering concurrency, crashed claims, temporary failure, repaired missing particulars and status exclusions. API/web typecheck/lint and diff checks pass. A live local simulated rent-payment browser test confirmed a persisted receipt before the user requested it (/tmp/rentos-auto-receipt-flow-recheck.log). The first browser run overlapped development reloads, showed no history rows and left its simulator payment pending; a fresh payment passed once the server was stable. Production restart/provider evidence remains separate.
- Fixed a related web defect: history request failures displayed an empty account. The page now shows a visible error and retry. A browser regression verifies failure is not reported as no payments and retry can load a genuinely empty result (/tmp/rentos-web-payment-history-error.log).
- Remaining: actual delivery and operational monitoring/escalation of deferred receipts, historical repair workflow, native share/print evidence, corrections/stamping/retention and wider C01–C10 requirements. The finalizer also performs wallet credit and legacy subscription activation after marking payments terminal and logs failures; their interruption recovery still needs an explicit end-to-end audit. Automatic receipt recovery does not repair those independent financial side effects or prove statutory delivery.

## Durable wallet-credit foundation — 13 September 2026

- Confirmed the payment finalizer marks rent/deposit payments completed before calling creditWallet, then skips all terminal retries. creditWallet itself credits on every call and its embedded history is capped at 500 entries. Simply retrying terminal payments or deduplicating against that display history would risk duplicate credits.
- Added WalletCredit journals with immutable operation key, beneficiary, amount, type and reference, plus pending/completed state. prepareWalletCredit rejects conflicting reuse and invalid/unrepresentable amounts. applyWalletCredit reserves a single bounded wallet slot, rechecks journal completion after reservation, and atomically mutates balance, transaction history and the slot's applied phase. It records durable journal completion before clearing the slot. A lost write acknowledgement can be resumed without repeating the balance movement. Claim identifiers fence delayed wallet updates; balance bounds and currency rounding apply to the new path. Literal references cannot become aggregation expressions.
- Verification: all 940 API tests across 92 files pass (/tmp/rentos-durable-wallet-api.log), with 12 real-Mongo cases covering concurrent preparation/application, conflicting reuse, prepared/applied interruption points, journal-write failure, completed-but-uncleared slots, delayed reservation after completion, history rollover, intervening debit and invalid amounts. API typecheck/lint and diff checks pass. Tests use isolated fixture wallets and perform no external money movement.
- Integration remains open: the payment finalizer still uses its original credit path. Next implementation must capture an immutable credit intent atomically with NEW payment completion, prepare the stable-key journal from that intent, and recover both pending journal rows and uncleared wallet slots (including slots whose journal already completed). Do not automatically enqueue historical completed payments: their previous credit outcome is ambiguous once history has rolled off. Add financial export/retention coverage before activating the new journal path. Subscription activation recovery, other transfers/refunds/payouts and operational reconciliation remain separate open work. No claim that the settlement interruption bug is fully fixed yet.

## Payment credit integration checkpoint — 13 September 2026

- New rent/deposit confirmations now capture a versioned immutable credit intent in the same atomic update as completed status. The update compares amount, parties, purpose and reference to the inspected record. Missing rent beneficiaries or invalid credit amounts reject completion for resolution. The intent records beneficiary, amount, type, reference and stable payment-derived operation key; recovery does not recompute those terms from later payment edits.
- The finalizer now prepares/applies the durable credit journal instead of directly calling the non-idempotent creditWallet function for rent/deposits. Preparation failures leave a recoverable intent. A five-minute job processes up to 50 intents per run with a next-attempt delay and also scans up to 50 occupied wallet slots, including journals completed before their wallet slot was cleared. Payment completion markers are recorded after durable application. Scheduled operation and operational alert thresholds still require deployment verification.
- Historical completed payments without explicit intents are deliberately excluded from automatic crediting; absence of a wallet display entry is not proof that money was never credited. Existing historical failures require reconciliation evidence. Legacy subscription activation and other monetary flows remain separate audit work.
- Beneficiary-only wallet credit journals are now included in account export. The export endpoint sends no-store. Account erasure retains this financial journal alongside existing financial records; the keys prevent replay after display history rolls off. Final lawful retention periods, closure handling, archival and disposal remain unresolved compliance gates, not claimed complete by retaining data indefinitely.
- Verification: all 946 API tests across 94 files pass (/tmp/rentos-wallet-credit-release-check.log). Five new real-Mongo integration cases cover rent/deposit beneficiaries, repeated confirmations, journal preparation outage, immediate post-confirmation recovery, immutable original terms and exclusion of legacy completions. A separate HTTP/Mongo export test with controlled session existence verifies positive own-journal inclusion, exclusion of another beneficiary despite a query impersonation attempt, and no-store. API typecheck/lint and diff checks pass. Live local simulated-payment browser evidence confirms both walletCreditCompletedAt and the automatic receipt before retrieval (/tmp/rentos-durable-payment-flow.log). No external money movement or production rollout was performed.

## Paid subscription purchase terms — 13 September 2026

- Audit found paid web subscriptions persisted only purposeMeta.packageId. Legacy activation reads the live package at settlement, computes expiry from that retry's current time, and does not pin the purchased version. Existing entitlement resolution also reads live legacy columns even when a version is pinned. Therefore snapshot capture alone is insufficient to close activation and grandfathering requirements.
- New paid checkout now captures versioned immutable subscriptionTerms before provider initiation: price/currency, package ID/version/name, billing cycle, benefits and the complete resolved feature set. The feature map is serialized as an immutable scalar to preserve dotted feature keys without mutable nested paths. Both Payment.amount and the provider collection amount use the captured price. These records are included with existing owned-payment account exports.
- Retry lookup now occurs before current package availability/free-plan handling. An existing purchase remains retrievable after package changes; matching requires subscription purpose, package and any supplied payment method. The duplicate-key race performs the same checks instead of returning an unrelated winner. Phone fingerprinting and provider-initiation response recovery remain separate open concerns.
- Verification: all 954 API tests across 95 files pass (/tmp/rentos-subscription-terms-api.log); API typecheck/lint and diff checks pass. Fourteen focused controller/Mongo cases include snapshot price/period/benefits/features, package/grant changes and deletion, nested/whole-record mutation attempts, invalid prices, retry independence from catalogue reads and conflicting duplicate-key winners. No provider calls are made by these tests.
- Remaining integration: prepare deterministic activation dates from confirmed payment, retain a durable activation state across interruption, prevent older completions overwriting newer subscriptions, and make entitlement/quota/subscription-summary consumers read the purchased snapshot. Legacy finalization still reads the live package, so purchased-term enforcement and subscription recovery are not yet claimed complete. Native store flows, free/admin plan assignments, existing ambiguous subscriptions and wider compliance gates require separate reconciliation.

## Saved-term paid subscription activation — 13 September 2026

- New paid payments with captured subscriptionTerms now use a dedicated activation path. It validates amount/account eligibility, prepares immutable coverage start/end dates from confirmed paidAt, and atomically writes the current user subscription plus the saved terms. Month/year additions clamp to valid calendar anniversaries. A lost payment completion-marker write can retry using the prepared dates, even if paidAt is later edited. Already expired coverage is recorded as expired without starting a fresh term.
- Conditional user updates prevent older completions replacing newer subscriptions. Equal-time purchases have a stable payment-ID ordering. A superseded payment is marked accordingly; suspended/deleted/ineligible accounts defer instead of receiving access. The five-minute recovery task processes bounded due batches and retries incomplete captured-term activations. Legacy purchases without saved terms retain their old path and are not automatically reinterpreted.
- Entitlements and subscription summaries use the saved feature/price/version/benefit data even if the catalogue package has changed or disappeared. Snapshot identity and coverage must match the current user's subscription; later manual assignments cannot inherit unrelated saved features. Paid access also checks the associated payment is still completed, removing the paid grant after a recorded refund. Store-versus-provider selection excludes inactive paid snapshots when considering a valid store subscription.
- Evidence: all 963 API tests across 96 files pass (/tmp/rentos-paid-subscription-final-api.log); API typecheck/lint and diff checks pass. Nine focused cases include calendar boundaries, missing catalogue records, summary/entitlement consistency, lost completion writes plus paidAt edits, older/equal-time ordering, expiry, invalid amount/suspension, refund denial and later manual assignment. A live local API checkout explicitly verified payment mode simulated before collection, then confirmed completed/applied state and exact agreement between the saved price/coverage and subscription summary (exec 40069 returned all assertions true). No external money movement or production/store release occurred.
- Remaining: full legacy subscription reconciliation; free/admin assignment and downgrade cleanup across all consumers; provider refund event delivery and operational recovery/alert verification; compensation for prolonged service outages; native store/device release evidence; and the remaining Ghana/legal/retention/product requirements. This checkpoint does not establish overall compliance or authorize a release.

## Subscription expiry and explicit assignment cleanup — 13 September 2026

- Expiry now uses the saved purchased price/name for captured-term subscriptions, so a removed or repriced catalogue record cannot prevent the expected downgrade. It checks the observed purchase ID and saved snapshot as well as package and expiry before changing the user. A stale expiry cannot erase a concurrent renewal even when package and expiry happen to match. Malformed saved terms remain unresolved rather than being reinterpreted from the catalogue.
- Successful downgrade/free-expiry cleanup removes subscriptionPaymentId and subscriptionSnapshotJson together with the old expiry. This prevents a stale paid snapshot from shadowing the configured free plan. Coverage expiry is recognized at its exact boundary; invalid fallback versions or clocks are rejected.
- Free checkout and admin assignments now share a conditional atomic assignment helper. It matches the observed package/dates/purchase identifier, clears paid metadata in the same write, pins the selected version and uses clamped calendar periods. A concurrent change produces 409 for refresh/retry. Explicit null legacy fields are matched as observed rather than confused with absent fields.
- Verification: all 967 API tests across 97 files pass (/tmp/rentos-subscription-lifecycle-api.log), including 13 focused expiry/assignment checks; API typecheck/lint and diff checks pass. Real-Mongo evidence covers missing catalogue records, configured free-limit enforcement after expiry, stale renewal protection, metadata removal and calendar assignment. A live API check first verified the isolated rentos_e2e database, then switched the fixture landlord from its saved paid plan to a free package and verified both metadata fields disappeared and the summary limit matched the chosen package. No production account or external payment was changed.
- Remaining: legacy subscription/financial reconciliation; catalogue and free/admin feature-version policy across all consumers; provider/store operational and device evidence; deferred recovery monitoring, lawful retention/disposal and the broader Ghana/product requirements. Overall goal remains IN PROGRESS.

## Consistent free entitlement fallback — 13 September 2026

- Found that accounts without subscriptions used the configured default free plan, while expired/refunded purchases used hard-coded registry defaults. For example, an expired subscription could resolve a three-property quota even when the configured free quota was one. Other explicitly configured free grants could disappear until the scheduled downgrade.
- The entitlement engine now uses the same active, zero-price default-plan lookup for expired/refunded saved purchases, expired or invalid legacy dates, missing catalogue packages and no-subscription accounts. Each fallback starts with fresh registry defaults and applies the free plan's current versioned grants; old paid features do not carry over. When no valid configured free plan exists, the existing registry defaults remain the fallback.
- Verification: all 972 API tests across 97 files pass (/tmp/rentos-free-fallback-final.log); API typecheck/lint and diff checks pass. Added coverage checks a configured one-property quota, explicit free blog grants, quota rejection at the boundary, invalid dates, missing plans, expired/refunded snapshots, and transition from a grandfathered paid version to the current free version. The initial full regression caught an older mock/expectation for the hard-coded fallback; it was updated to exercise the configured free-plan behavior.
- Remaining: PropertyService still has its separate expired-subscription renewal gate, and subscription-summary presentation currently distinguishes inactive paid coverage from the engine's free fallback. Those product/access-path rules need explicit reconciliation; this checkpoint only establishes consistent feature resolution, not end-to-end equivalence across every UI and domain gate. Other Ghana/store/financial/retention obligations remain open.

## Subscription summary and property quota parity — 13 September 2026

- Removed the separate blanket property-creation denial for expired subscriptions. PropertyService now enforces the effective property.limit through requireQuota for all subscription states. A user below the free quota can add a property after paid coverage ends; one at or above it cannot. Existing properties are not deleted by this change.
- Extracted shared configured-free resolution and plan-version entitlement resolution for the summary. Expired/refunded/missing-plan accounts now receive the effective free package/limit with fallbackApplied and previousSubscriptionInactive metadata. isExpired describes effective returned access rather than labeling the free fallback expired, and old paid coverage dates are omitted from that fallback summary. Active legacy package summaries use versioned property grants instead of only the mutable maxProperties column. Active store and saved paid snapshots retain their existing terms.
- Added OpenAPI documentation for the authenticated summary response and these distinctions. No API response is treated as proof of purchase or statutory compliance.
- Verification: all 977 API tests across 98 files pass (/tmp/rentos-subscription-access-api.log), with focused summary rechecks after metadata/documentation refinement. API typecheck/lint and diff checks pass. Property-service cases verify creation below and rejection at the one-property free limit after expiry; controller cases verify matching limits, versioned overrides, expired/refunded fallback and capacity reporting. These are domain/controller checks with mocked storage, not a new full browser/native journey.
- Remaining: full browser/native expiry/refund journeys; concurrency protection for the count-then-create quota path; legacy/operational reconciliation; and the remaining Ghana/store/privacy/financial requirements. Overall goal remains IN PROGRESS.


### Property quota concurrency — 13 September 2026

- Replaced the normal property service count-then-create race with immutable numeric slots and a unique partial `(landlordId, quotaSlot)` index. Creation establishes the index before proceeding, and fails closed if that operation fails. Competing inserts retry slot-specific duplicate keys up to eight times, rereading inventory and effective entitlement; exhausted contention returns 409. Other database errors propagate.
- Legacy unslotted documents remain counted. Deleting a property removes its reservation atomically with the property; failed validation leaves no reservation. No expiring lock or separate quota counter can become detached from the insert.
- Evidence: four real-Mongo tests cover final-slot contention with a legacy document, concurrent empty-account admission, deletion/reuse and slot immutability, and failed validation. Full API suite passed 981 tests in 99 files (`/tmp/rentos-property-quota-full.log`); the subsequently added fail-closed index regression passed with all seven service tests (`/tmp/rentos-property-quota-unit.log`). API typecheck and lint passed.
- Scope: normal runtime property creation uses this service. Index creation privileges and existing-index reconciliation must be verified at deployment. Direct database/import writers, concurrent subscription downgrades, and projected-inventory scale require further review; this change does not certify legal or store compliance.


### Collection initiation ordering — 13 September 2026

- Found subscription checkout marking any exception after Payment creation as failed, even when a provider may already have accepted the collection or a successful webhook has finalized it. Replaced that stale-document save with a conditional update limited to pending/processing records. Uncertain attempts remain processing, retain their idempotency key and record a timestamp without saving raw provider exception data.
- Rent and subscription initiation now write provider metadata conditionally and reread the payment for the response. Terminal webhook state and provider evidence win over delayed initiation results. Provider instructions are persisted and returned for matching retries, including duplicate-key winners.
- Evidence: real MongoDB tests cover completed-before-error, completed-before-initiation-result, uncertain-state/instruction persistence and ten concurrent completion/error races. Updated subscription and rent controller tests exercise the new persistence boundary. All 986 API tests across 100 files pass (`/tmp/rentos-initiation-full.log`); API typecheck/lint pass.
- Remaining: an exception does not establish whether money moved. Initiation is not yet a durable recoverable outbox, and a process crash before saving a provider reference can leave polling without a correlator. Live rail-specific query/idempotency contracts, bounded recovery, historical failed-payment reconciliation and customer-facing uncertain-payment recovery still require implementation/verification. No provider collection or external charge was performed for these tests.


### Client checkout retry persistence — 13 September 2026

- Web and mobile previously sent no Idempotency-Key for rent/subscription provider checkouts. A shared coordinator now persists a cryptographic random UUID before sending and hashes owner, endpoint and payload for lookup. Matching concurrent requests share one network operation; exceptions retain the original key, successful responses resolve it, and storage failures stop collection before sending. Native storage uses SecureStore; browser storage uses localStorage with Web Locks protecting persistence operations. Free subscriptions and receipt issuance are excluded.
- Provider checkout requests carry the key through token refresh. Web API responses and retries check originating account identity, and delayed refresh results cannot replace a different account's credentials.
- Verification: six shared request regressions pass; web browser checks prove a matching key after network failure/reload and no checkout retry/refresh following a replacement-account 401. Two Expo-web component checks prove original instructions remain visible and a retry reuses its key. Web/mobile typecheck and lint pass. Test collections are intercepted; no real payments were initiated.
- Expo Crypto ~55.0.19 added per https://docs.expo.dev/versions/v55.0.0/sdk/crypto/; dependency audit reports zero vulnerabilities. Metro watches shared runtime code; it was restarted successfully in exec 72653, port 8581. Native rebuilt-binary/SecureStore integration verification remains pending.
- Remaining: matching payloads are protected, but edited-payload retries and cross-device attempts need explicit recovery/coordination. Clearing app/browser storage loses local retry state. Records for permanently abandoned failures need an appropriate recovery/retention workflow. Server initiation outbox, lost provider references and live rail reconciliation remain open.

Checkout verification follow-up: all 32 mobile checks passed after Metro configuration and dependency integration (`/tmp/rentos-checkout-mobile-full.log`). Two web browser checks and six shared coordinator checks remain green. CocoaPods installed ExpoCrypto 55.0.19 successfully (111 total pods); simulator build exec 70744 completed successfully.

Native follow-up: signed Debug iOS simulator build succeeded (`/tmp/rentos-checkout-ios-build.log`), installed on dedicated simulator CF9ACF95-0176-41C3-9038-7B46CB5CE620 and launched as gh.rentos.mobile. The current Metro bundle rendered the login screen, including loading its API/Crypto imports, without a missing-module error (`/tmp/rentos-checkout-native-launch.png`). This proves build/install/startup compatibility; it does not prove authenticated native payment, persisted checkout across native process death, store sandbox billing or final archive privacy declarations. No login/landing redesign was made in this checkpoint.


### Bounded checkout waits — 13 September 2026

- Shared provider checkout coordination now limits the network/response wait to 30 seconds, passes an AbortSignal to web/native API clients, and races against a rejection so an uncooperative transport cannot keep the form pending forever. Client abort does not imply that the server/provider stopped processing. Timeout/cancellation therefore retains the original key and instructs the payer to retry the same details to check the original attempt. A late result cannot clear the retained key. Caller cancellation before dispatch prevents the collection call.
- Evidence: eight shared coordinator regressions and the two existing web browser checks passed (`/tmp/rentos-checkout-timeout-tests.log`); an additional clock-controlled browser test stalled checkout, advanced past the deadline and verified identical-key retry (`/tmp/rentos-checkout-stalled-browser.log`). Two affected Expo-web payment component checks passed; web/mobile typecheck, lint and diff checks passed. No real collections occurred.
- Scope: the deadline bounds the send/response phase; storage preparation still depends on platform storage responsiveness. Exact-payload retries are protected. Changed payloads yield a different lookup hash/key, and cross-device attempts or cleared storage are not coordinated. Those require an explicit server-backed recovery workflow and remain open with provider-reference/outbox recovery and authenticated native verification.


### Financial evidence for reconciliation — 13 September 2026

- Found the scheduler constructing a completed event using the stored expected amount and current polling time after a status-only query. That bypassed amount verification and could extend subscription coverage incorrectly. It now requests verified collection facts and checks reference, GHS currency, positive finite amount, and a valid nonfuture provider paid timestamp before completing. The observed amount flows to the finalizer, so a mismatch is held for review instead of silently replaced.
- Paystack uses the documented transaction verification fields, converting integer subunits to major GHS units and discarding customer/authorization data. Sources: https://paystack.com/docs/api/transaction/ and https://paystack.com/docs/payments/accept-payments/ (reviewed 13 September 2026). Query failures remain unresolved with a coarse log message.
- Status-only adapters cannot authorize scheduled settlement until they implement verified financial evidence. Existing authenticated webhook processing is still available. Simulator polling now returns pending for unfamiliar references instead of treating live-looking references as completed. This closes a dangerous mode-change path.
- Finalizer amount checks now require positive safe integer pesewa values and equality; non-finite provider values and one-pesewa mismatches cannot bypass validation.
- Evidence: all 1,008 API tests in 101 files pass (`/tmp/rentos-financial-verification-full.log`), including real-Mongo underpayment/no-credit checks and non-finite/nonpositive/one-pesewa regressions. API typecheck, lint and diff checks pass.
- Remaining: direct MTN/Telecel/AirtelTigo verified-financial query contracts and live sandbox evidence, saved adapter/rail identity, provider reference persistence before initiation, restart-safe initiation recovery, currency validation across other webhook paths, and historical reconciliation. No live provider calls or real financial operations were performed. Overall compliance remains IN PROGRESS.


### Webhook currency validation — 13 September 2026

- Provider events previously discarded currency, allowing the same numerical amount in a foreign or unknown currency to pass the GHS finalizer. Normalized events now carry the provider-reported currency; Paystack, direct telco and bank parsers preserve it without inventing a default. Simulator-generated GHS collections explicitly identify their currency. Financial reconciliation also passes its verified currency into the event.
- Completion requires currency exactly GHS. Missing or foreign values hold the nonterminal payment in processing with a coarse currency-unverified reason, and produce no settlement/credit intent. A subsequent valid GHS confirmation may complete normally; successful completion clears obsolete failure and initiation-uncertainty metadata. Previously terminal records remain immutable under replay.
- Evidence: all 1,015 API tests across 101 files passed (`/tmp/rentos-webhook-currency-tests.log`), including real-Mongo missing/foreign/lowercase currency rejection, no wallet credit while held, and correct once-only recovery after GHS confirmation. API typecheck, lint and diff checks pass. The local browser rent-payment flow passed through confirmation, wallet credit and receipt rendering (`/tmp/rentos-webhook-currency-browser.log`); it now explicitly checks simulated mode before initiating payment.
- Limitations: direct-provider live callback schemas must supply confirmed currency or obtain equivalent verified financial evidence; absent fields now remain unresolved instead of being assumed GHS. Live sandbox/provider contracts, historical currency reconciliation, failure-event currency handling, callback-reference/rail binding, provider-reference crash recovery and the broader legal/store gates remain open. No real money moved.


### Pending webhook ordering — 13 September 2026

- Replaced the pending-event document save with a conditional update matching nonterminal payment state and the provider reference observed at read time. A concurrent completion or reference assignment now prevents the stale metadata write. Missing and explicitly null legacy references can still receive their first pending reference.
- Finalization now explicitly accepts only pending/completed/failed normalized statuses. An unsupported runtime value is ignored before payment lookup, rather than falling through to the completion path.
- Evidence: five new real-Mongo regressions force completion/reference changes between the read and write, cover missing/null references, and verify unsupported statuses leave payment and wallet state unchanged. All 1,020 API tests across 101 files pass (`/tmp/rentos-pending-event-full.log`); API typecheck, lint and diff checks pass.
- Remaining: provider/rail identity binding, ambiguous provider-reference fallback, failed-versus-successful terminal event reconciliation, live callbacks, pre-initiation correlator persistence and broader legal/store verification remain open. No real payments or external provider operations occurred.


### Collection source binding and callback retries — 13 September 2026

- The three normal Payment collection writers (rent, subscription, wallet deposit) now select the provider before persistence and save its immutable collectionSource. API body fields do not select that source. Paystack, each direct rail, and the simulator identify their own adapter.
- Finalization of tagged payments requires a matching providerSource supplied by the verified server callback route or simulator bridge. A Paystack callback cannot settle a direct/simulated payment and vice versa. Scheduled reconciliation resolves the saved source rather than current mode/rail defaults. Invalid saved source/method combinations fail instead of selecting an unrelated adapter.
- Provider-reference-only fallback now reads at most two matching records and refuses ambiguous matches. Exact RentOS reference lookup remains preferred. This is not a uniqueness guarantee for provider references across concurrent inserts; provider-reference index/migration work remains open.
- Direct-provider callback processing exceptions return HTTP 503, asking for retry rather than acknowledging an unprocessed event. Signature failures remain HTTP 401; an idempotently processed callback is acknowledged normally. Actual retry behavior still depends on provider contracts. Paystack marketplace durable delivery/replay has its own path requiring separate verification.
- Evidence: real-Mongo source immutability, wrong/missing-source rejection, correct-source credit, and ambiguous-fallback tests; provider selection tests across config changes; real HTTP callback tests for authentication, trusted source and 503/retry acknowledgement; controller assertions that source is persisted before provider calls. Final full suite: 1,025 tests across 103 files pass (`/tmp/rentos-source-recheck.log`); API typecheck/lint and diff checks pass. The simulated browser rent-payment/credit/receipt flow passed (`/tmp/rentos-source-browser.log`).
- Verification caveat: an earlier full run had eight invitation HTTP tests return 400; the focused 28-test rerun and subsequent full suite passed. No invitation implementation change was made and the transient failure's cause is unproven.
- Remaining: historical payments without a saved source retain legacy behavior pending evidence-backed migration; provider/account key rotation, unique provider references, durable pre-initiation correlators/outbox, Paystack callback replay, native recovery and broader Ghana/store gates remain open. No live provider collections or deployments occurred.


### Wallet deposit checkout recovery — 13 September 2026

- Wallet deposit creation previously bypassed idempotency, rail-availability checks and the conditional initiation recording used by rent/subscription payments. It now rejects unavailable rails before creation, rejects rounded-zero/unsafe amounts, saves an owner-scoped idempotency key, and resolves matching retries or duplicate-key creation races to the original payment/instructions. Conflicting purpose/method/amount is rejected. Existing attempts remain retrievable after a rail becomes unavailable.
- Provider initiation results use the shared conditional recorder; uncertain failures retain a processing attempt instead of claiming failure or overwriting a terminal callback. Shared web/native provider-checkout coordination now includes /savings/wallet/deposit, giving matching deposits persisted retry keys, deduplication and bounded waits. Withdrawals are excluded and continue through payouts.
- Evidence: ten deposit controller regressions cover ordered persistence, retry/race/conflict handling, availability, uncertain initiation and invalid amounts. All 1,035 API tests in 104 files pass (`/tmp/rentos-deposit-full.log`); eight shared coordinator checks pass; API/web/mobile typechecks and API lint pass.
- Browser evidence: after explicitly checking simulated mode, a local GHS 1 deposit was accepted by the API and its response deliberately lost. The browser API client retried with the same key and received the original Payment id. The saved credit marker and wallet delta confirmed exactly one credit (`/tmp/rentos-deposit-browser.log`). This exercised client/API/simulator/database recovery, not the native deposit form or a real financial provider.
- Remaining: native deposit form and cold-restart evidence, dynamic rail presentation, edited-payload/cross-device retries, provider-reference/outbox recovery, wallet safeguarding/licensing/operating arrangements and other Ghana/store gates. No real money moved.


### Deposit form availability and instructions — 13 September 2026

- Removed the fixed web deposit payment-method list and switched mobile deposits to the API-provided list. Both forms expose loading/unavailable/empty states, provide a retry action, preserve the entered amount on retry and prevent submission with an unavailable selection. Mobile ignores method responses after the form is closed or the request is superseded. Withdrawal options remain a separate payout concern.
- Mobile deposit instructions are now retained in a dismissible on-screen message after submission, in addition to the platform alert, so reference/account instructions are not lost when the modal closes or alerts are unavailable on Expo web.
- Evidence: web browser form test verifies load-error recovery, entered-amount preservation, hidden unavailable MTN method and enabled bank selection. Two new Expo-web component checks verify mobile method filtering, retry preservation and persistent instructions. All 34 mobile checks pass (`/tmp/rentos-deposit-methods-mobile-full.log`); web form check passes (`/tmp/rentos-deposit-methods-web-final.log`); web/mobile typecheck and lint pass. No live collections occurred in these tests.
- Remaining: native-device and cold-restart deposit verification, pending/confirmed instruction refresh, wallet error-state review, edited-payload/cross-device recovery, withdrawal rail presentation, and broader legal/store operating gates.


### Wallet unavailable states — 13 September 2026

- Web and mobile savings views now distinguish unavailable wallet/savings requests from an actual zero balance or empty history. Failed loads show a visible retry state and hide financial values until successful recovery. Retry reloads wallet and plans.
- Web wallet query and mobile loader reject responses lacking a finite numeric balance or transaction array. Mobile also requires a plans array and ignores responses/errors from superseded load generations. Initial failure and incomplete-success responses no longer silently become zero/empty data.
- Evidence: three web browser checks passed (`/tmp/rentos-wallet-error-web-final.log`) and four focused Expo-web checks passed (`/tmp/rentos-wallet-error-mobile-final.log`), covering outages, incomplete responses, recovery and the existing deposit method/instruction flow. Web/mobile typecheck, lint and diff checks pass.
- Remaining: complete financial response-schema validation, validation/error behavior in other wallet consumers, authenticated native-device refresh/offline recovery, storage/process-death recovery and the broader Ghana/store operating gates. No real financial operations occurred.


### Withdrawal and insurance wallet recovery — 13 September 2026

- Web withdrawal availability now rejects missing/non-finite balance or minimum and invalid account/progress flags. An unavailable or incomplete response presents an explicit retry state instead of incorrectly instructing the user to add a payout account. Submission requires valid availability, a verified account, no existing payout in progress and a finite positive amount within the returned limits.
- Insurance checkout now renders wallet loading and retry states separately from insufficient funds. Purchase submission is guarded until wallet data is available and the first monthly premium is covered; the backend charges that first premium at purchase.
- Evidence: two withdrawal browser checks cover HTTP failure and incomplete-success recovery (`/tmp/rentos-payout-availability-browser.log`); one insurance browser check covers wallet outage and retry (`/tmp/rentos-insurance-wallet-browser.log`). Web typecheck and lint pass (`/tmp/rentos-financial-consumers-types.log`, `/tmp/rentos-financial-consumers-lint.log`). These tests mock financial responses and do not submit payouts or purchase policies. API code was unchanged; the prior full API result remains 1,035 passing tests across 104 files.
- Remaining: native withdrawal method/account setup presentation, complete financial response-schema validation, authenticated native offline/restart evidence, provider recovery and broader Ghana/legal/store obligations. These UI safeguards do not establish legal certification, licensing or store approval.


### Native withdrawal availability and saved destination — 13 September 2026

- Removed the mobile withdrawal payment-method selector and its required-selection guard: POST /payouts accepts an amount and uses the saved verified payout account. Deposit selection remains independent and API-driven.
- Opening withdrawals now fetches and validates payout availability. Loading/error/incomplete responses prevent submission; retry is visible. Missing account and existing-payout states explain the prerequisite. Valid availability displays minimum/balance and the approval/refund flow. Submission guards reject non-finite, below-minimum and over-balance values and ignore repeated calls while submitting. Closed/superseded availability requests cannot update the modal.
- Four focused Expo-web component checks pass (`/tmp/rentos-native-payout-browser.log`) for unavailable/incomplete recovery, amount-only submission to a mocked payout endpoint, missing account and existing payout. Mobile typecheck/lint pass (`/tmp/rentos-native-payout-types.log`, `/tmp/rentos-native-payout-lint.log`). Expanded amount-boundary assertions reject below-minimum, above-balance and infinite values. All 40 mobile regression checks pass (`/tmp/rentos-native-payout-full.log`), including existing deposit, billing, receipt and session flows. Diff checks pass.
- Remaining: native payout-account setup is not implemented here; missing-account copy directs users to the existing web Settings/Payouts flow. Authenticated device/offline/process-death and real-provider payout verification, complete financial schema validation and broader Ghana/legal/store gates remain open. No real payout or provider operation occurred.


### Native payout-account setup — 13 September 2026

- Added a native payout-account route, Profile entry and direct setup action from missing-account withdrawals. Users can select an API-provided mobile-money network or bank, submit account details for server/provider verification, view confirmed account details, replace the destination and remove it with inline confirmation. Details remain in component memory and are cleared after successful saving/removal; no new persistent client storage was introduced.
- Account/destination loading distinguishes unavailable or malformed data from no saved account, blocks the form until valid responses arrive and exposes retry. Save requires a matching supported destination and bounded account fields. Verification failures preserve input; success requires a valid verified account response. Removal explains that existing payouts retain their snapshot destination.
- Initial setup browser check passed after correcting the test fixture to use the API error field rather than message. Four withdrawal checks also passed. Final expanded run: all five focused checks pass, including replacement, withdrawal-to-setup navigation, failed-load/verification recovery and removal confirmation (`/tmp/rentos-payout-setup-verified.log`). Diff checks pass. Mobile typecheck/lint pass (`/tmp/rentos-payout-setup-types.log`, `/tmp/rentos-payout-setup-lint.log`).
- Remaining: authenticated native-device and live-provider account verification, account change authorization/race auditing, provider-recipient recovery and broader Ghana/legal/store operating gates. Tests use mocked APIs and do not create real provider recipients or send money.


### Web payout-account response and error handling — 13 September 2026

- Web payout queries now validate account and destination response shapes. Failed/incomplete account loads display retry and cannot masquerade as a missing account. Failed destination loads expose retry; save requires a matching available destination and bounded input lengths. Save responses must contain a complete verified account before the UI reports verification success.
- Added persistent inline save/removal errors. A browser regression initially found the save error invisible; inspection shows PayoutTab imports react-hot-toast while DashboardLayout renders a separate toastStore-based Toaster. Inline feedback fixes this form's error visibility; reconciling the shared toast implementations and success feedback across other callers remains open.
- Evidence: all three browser checks pass (`/tmp/rentos-web-payout-account-final.log`), covering failed/incomplete account recovery, destination retry with retained input and incomplete verification rejection. Web typecheck/lint and diff checks pass (`/tmp/rentos-web-payout-account-types.log`, `/tmp/rentos-web-payout-account-lint.log`). Mocked APIs only; no provider accounts or payments were created.
- Remaining: shared toast reconciliation, live provider/device verification, payout-account authorization/race review, provider recovery and broader Ghana/legal/store obligations.


### Shared web notification delivery — 13 September 2026

- Replaced the disconnected toastStore renderer with one react-hot-toast renderer at the application root. Existing useToastStore callers forward into the same library, while direct library callers require no migration. Public/auth screens and dashboard screens now share notification rendering without duplicate dashboard instances.
- Errors persist until dismissed; all non-loading notifications have a labelled dismiss button. Error messages render with alert/assertive semantics. The first browser check exposed the library's per-toast aria defaults overriding configured defaults; the renderer now explicitly applies the error aria properties to ToastBar.
- Implementation references: official [Toaster documentation](https://react-hot-toast.com/docs/toaster) and [toast API](https://react-hot-toast.com/docs/toast), with installed source inspected to verify option precedence. Existing page-level error messages remain persistent too.
- Verification: all five public notification/payout recovery checks pass (`/tmp/rentos-notifications-final.log`), proving legacy/public error delivery, single-render persistence/dismissal, direct-library success and retained payout error handling. Web typecheck/lint and diff checks pass (`/tmp/rentos-notification-types.log`, `/tmp/rentos-notification-lint.log`). No real financial or provider operations occurred.
- Remaining: session-boundary notification cleanup, caller-specific loading/error lifetime review, wider runtime flow coverage and broader Ghana/legal/store gates.


### Notification cleanup at session boundaries — 13 September 2026

- Web login/logout actions immediately remove queued/displayed notifications, including a fresh login for the same account. A store subscription also clears notifications when user identity or authenticated state changes through hydration/direct replacement. Access-token rotation and ordinary same-account profile updates retain current messages. Development hot reload disposes the subscription before replacement.
- Evidence: all five notification browser checks pass (`/tmp/rentos-notification-session-browser.log`). Three new cases verify token/profile preservation followed by logout, direct account replacement or fresh same-user login cleanup. Existing public error persistence/dismissal and direct payout-success notification checks remain green. Web typecheck/lint and diff checks pass (`/tmp/rentos-notification-session-types.log`, `/tmp/rentos-notification-session-lint.log`).
- Scope: this clears notifications already present at the transition. Late asynchronous callbacks can still create new notifications unless their calling flow guards the originating session; auditing those callers remains open. The separate auth-store refresh/rehydration path also needs its own stale-session review. Broader provider/native/legal/store obligations remain open; no real financial operations occurred.


### Web restored-session race protection — 13 September 2026

- Auth restoration captures the originating user id, token pair, authenticated flag and explicit login/logout generation. Response, body parse, refresh and commit paths check that snapshot and effect lifetime before mutating state. Old responses cannot restore logged-out credentials or overwrite a replacement/same-user fresh login.
- Refresh returns credentials without unguarded writes. Once checked, rotated credentials are retained and trigger a fresh guarded profile check, so a subsequent profile outage does not discard the rotated refresh token. Profile responses must have a user id/roles and match the known account. HTTP 401/403 can end the current session; outages/malformed responses preserve it. Removed the one-shot validation latch that could prevent restarted effects from validating.
- Five browser checks pass (`/tmp/rentos-rehydrate-final.log`): delayed refresh versus logout, replacement and same-user login; ordinary rotation; persisted-session server outage. Initial race tests imported a fresh unversioned auth module under Vite hot reload and manipulated a separate store. Tests now locate and import the application's loaded resource URL. Earlier notification/checkout race tests were corrected the same way; all eight rerun checks pass (`/tmp/rentos-session-regressions.log`). Web typecheck/lint and diff checks pass (`/tmp/rentos-rehydrate-types.log`, `/tmp/rentos-rehydrate-lint.log`).
- Remaining: coordinated concurrent refresh between API client/restoration, complete same-user generation guarding in other API/callback paths, transient verification UX and wider auth/native/provider/Ghana/store obligations. No live financial operations occurred.


### API requests across same-account login boundaries — 13 September 2026

- Exported the auth store's in-memory session generation and applied it to API request delivery, response/body handling, checkout callbacks and AI-permission boundaries. A new explicit login invalidates older work even if user id and credential strings are identical. Token rotation within a session remains allowed.
- Uploads now use the same guarded request/refresh path as JSON requests. FormData omits the JSON Content-Type header so the browser generates the multipart boundary. Late old-session successes/401s cannot deliver data or retry uploads as a new session.
- API refresh deduplication is scoped to the login generation; old promise completion cannot clear a newer attempt. Non-authentication HTTP failures, network failures and malformed refresh success responses preserve the session and surface errors rather than forcing logout. Provider rejection still expires the current session.
- Evidence: eight browser checks pass (`/tmp/rentos-api-session-browser.log`), covering JSON/upload late success and 401 after same-account login, multipart boundary generation, refresh outage preservation and three checkout regressions. Web typecheck/lint and diff checks pass (`/tmp/rentos-api-session-types.log`, `/tmp/rentos-api-session-lint.log`). The additional old/new refresh overlap check passes (`/tmp/rentos-api-refresh-isolation.log`): a newer same-account login completes its own refresh while the old refresh remains pending, and old rejection does not log out the newer session.
- Remaining: coordinate refresh across the API/restoration paths and tabs, guard every delayed UI callback, and verify broader native/provider/Ghana/store obligations. Tests use mocked session/upload endpoints and do not transmit real user files or payments.


### Shared refresh coordination for web restoration and API requests — 13 September 2026

- Removed the API client's separate refresh transport/promise. Both the API client and restored-session hook call a coordinator in the auth store, sharing one pending refresh within a login generation. A rejected access token is passed explicitly; if another request has already rotated it, callers retry with current credentials instead of rotating again.
- The coordinator validates the original identity, authentication state, generation and credential pair before committing returned credentials. It retains the existing bounded transport and temporary-error/rejected-credential distinction. Old generations cannot join or clear newer refresh attempts. Successful rotation triggers a fresh guarded profile verification.
- Verification: six API boundary checks passed in `/tmp/rentos-shared-refresh-browser.log`; five restoration checks passed on rerun in `/tmp/rentos-shared-refresh-final.log`, including overlapping restoration/API requests completing through exactly one refresh. Initial setup timeouts were resolved by installing expired credentials before application scripts, avoiding live-store overwrites. The separate late-401 check passes in `/tmp/rentos-late-refresh-final.log` after isolating unrelated page API traffic from mock credentials; both requests recover with exactly one refresh. These are focused results across runs, not a claim that the earlier combined runs were fully green. Web typecheck/lint pass (`/tmp/rentos-shared-refresh-types.log`, `/tmp/rentos-shared-refresh-lint.log`).
- Remaining: browser-tab/process coordination, response-loss recovery after server rotation, complete auth-caller lifetime review and broader native/provider/Ghana/store obligations. No real financial operations occurred.


### Authentication synchronization between browser tabs — 13 September 2026

- Each explicit web login now persists an opaque UUID session id; logout clears it. The auth subscription advances the in-memory generation when this id changes, including same-account login in a different tab. Existing notification and request guards therefore react to remote session transitions too.
- Added a storage listener for the auth key or storage clear. It re-reads the latest stored state rather than replaying a potentially stale event payload. Removing stored authentication logs out other tabs; rotated credentials/profile changes within the same persisted session synchronize without advancing the login generation. Hot reload removes the old listener.
- Evidence: four actual two-page browser-context checks pass (`/tmp/rentos-tab-session-browser.log`): remote logout, same-user login and auth-key removal reject a held old request; same-session refresh-token changes synchronize without generation changes. Web typecheck/lint and diff checks pass (`/tmp/rentos-tab-session-types.log`, `/tmp/rentos-tab-session-lint.log`). All five restoration regressions also pass (`/tmp/rentos-tab-restoration-regression.log`), including shared refresh and stale-response protection.
- Remaining: serialize refresh calls across tabs, handle lost refresh responses/process crashes, audit stale persisted-state writers and legacy tabs lacking the new id, and complete wider native/provider/Ghana/store obligations. No real financial operations occurred.


### Cross-tab refresh locking — 13 September 2026

- Web refresh now uses an exclusive browser lock named with the opaque persisted login id. Waiting tabs rehydrate current storage after acquiring the lock and reuse already-rotated access credentials. Storage is checked again before committing returned credentials; removed authentication is treated as logout. New login ids use independent locks, so obsolete refresh work cannot block a new session.
- Lock acquisition has a 10-second timeout; refresh transport retains its own 4-second limit. Browsers without Web Locks retain the same-tab coordinator but do not gain cross-tab serialization. API semantics were checked against [MDN LockManager.request](https://developer.mozilla.org/en-US/docs/Web/API/LockManager/request).
- Two focused browser checks pass (`/tmp/rentos-tab-refresh-focused.log`): two actual tabs recover two expired API requests with exactly one refresh and both receive the rotated credentials; a new login refreshes independently of an older pending session. The first in-development run observed two refresh calls, so the final code was rerun without further runtime edits: all ten tab/restoration checks pass (`/tmp/rentos-tab-refresh-final.log`). The earlier run is not counted as green. Web typecheck/lint pass (`/tmp/rentos-tab-refresh-types.log`, `/tmp/rentos-tab-refresh-lint.log`).
- Remaining: unsupported-lock browser behavior, stale persisted-state writers, response loss after server rotation, process-crash recovery and broader native/provider/Ghana/store obligations. Tests use mock credential endpoints; no real financial operations occurred.


### Delayed profile responses before storage-event delivery — 13 September 2026

- Reproduced a cross-tab session overwrite: suppressing storage-event delivery allowed the first tab's delayed profile response to restore stale account data after another tab logged out or started a fresh login. The baseline browser run failed both new cases (`/tmp/rentos-stale-profile-before.log`); the rendered page showed the stale profile after remote logout.
- Restoration's response guard now compares the captured session with persisted user id, authenticated state, session id and credential pair before accepting responses/body data or applying updates. A mismatch rehydrates the latest stored state and rejects the old response; removed authentication logs out, and unreadable storage does not authorize a write.
- Final verification: all twelve browser cases pass (`/tmp/rentos-stale-profile-final.log`): seven cross-tab cases plus five restoration cases, including both regressions with deliberately suppressed storage events. Web typecheck/lint and diff checks pass (`/tmp/rentos-stale-profile-types.log`, `/tmp/rentos-stale-profile-lint.log`).
- Remaining: other delayed state writers, atomic coordination of writes across processes, metadata-update ordering, lost refresh responses and broader native/provider/Ghana/store obligations. These tests use mock profile responses and do not perform real financial actions.


### Query-cache isolation across sessions and roles — 13 September 2026

- Reproduced cached wallet transactions remaining visible after a fresh login to both the same and a different account. Both baseline browser cases failed (`/tmp/rentos-cache-before.log`): the prior transaction remained rendered because the query key was shared and its five-minute cache lifetime survived authentication changes.
- Moved the query client to a dedicated module with a synchronous auth subscription. Login id, user id, authenticated state or active-role changes clear query and mutation caches. The provider is keyed to the same boundary, remounting observers and local form state so old rendered data cannot survive while replacement requests are pending. Token rotation and ordinary profile changes do not alter this key.
- Evidence: seven session/notification browser checks pass (`/tmp/rentos-cache-final.log`), including both reproduced failures now hiding the prior transaction until the new wallet response arrives. The additional active-role transition check passes (`/tmp/rentos-role-cache-final.log`). Web typecheck/lint and diff checks pass (`/tmp/rentos-cache-types.log`, `/tmp/rentos-cache-lint.log`). Tests use mocked wallet data; no real financial operations occurred.
- Remaining: other client stores and browser persistence, late mutation callbacks, exhaustive route/form verification, cross-process state races and broader native/provider/Ghana/store obligations. Clearing a mutation cache does not undo a server operation already submitted.


### Mobile cache and navigation session boundaries — 13 September 2026

- Mobile used a singleton query cache without session invalidation. Added a session-aware query-client factory that clears query/mutation caches on login generation, user identity, authentication or active-role changes. The root navigation stack uses the same boundary key to discard old screen-local state and observers.
- Root credential hydration remains outside the keyed stack. This avoids remounting AuthGuard and repeatedly hydrating/incrementing the session generation. Token rotation and ordinary profile metadata changes preserve the cache boundary.
- Added five regression cases against the actual query client: login/logout/role eviction with fresh replacement fetches; cancellation of a pending old query whose late response cannot repopulate cache; token/profile update preservation. These are cache-contract tests with a controlled auth subscription, not authenticated native-device switching evidence. All 46 mobile regression checks pass (`/tmp/rentos-mobile-cache-final.log`), including the five new cache cases and existing Expo-web component flows. Mobile typecheck/lint and diff checks pass (`/tmp/rentos-mobile-cache-types.log`, `/tmp/rentos-mobile-cache-lint.log`).
- Remaining: authenticated native-device switching/process death, other client stores and persistence, late mutation callbacks, development hot-reload subscription lifecycle and broader provider/Ghana/store requirements. No real financial actions occurred.


### Web favorites and notification-store session cleanup — 13 September 2026

- Favorites and stored notification records previously depended on the header logout handler for cleanup. Added auth-generation subscriptions so logout, fresh same-account login, replacement and propagated remote-session changes reset these stores independently of the navigation path. Hot reload disposes the subscriptions.
- Favorite requests capture their originating session generation. Late loads, toggle successes, error rollbacks and toggling-state cleanup cannot mutate a newer session. This specifically prevents a failed old removal request from putting the previous account's property back into the new account's favorites.
- Evidence: five browser checks pass (`/tmp/rentos-private-store-browser.log`). Two real-client store checks hold favorite responses across a fresh login and release HTTP 200/500, verifying no old property returns and notification records/unread count are empty. Three wallet cache/session/role checks remain green. Web typecheck/lint and diff checks pass (`/tmp/rentos-private-store-types.log`, `/tmp/rentos-private-store-lint.log`). Financial data and favorite endpoints are mocked; no real account preferences were changed.
- Remaining: mobile notification/socket state, other persistence, load-error presentation and within-session favorite concurrency, delayed unrelated callbacks and broader native/provider/Ghana/store gates.


### Mobile notification state and queued callbacks — 13 September 2026

- Mobile auth login, logout and successful persisted-session hydration now clear notification banners and unread counts. Existing hydration generation checks run before clearing, so an obsolete hydration attempt does not reset a newer session.
- Added disposable session callback guards to initial/foreground profile and unread-count requests, socket unread/new-notification events and account-suspension events. Effects track sessionVersion as well as token/authentication. Cleanup invalidates queued callbacks before detaching listeners; mismatched profile user ids are ignored.
- Three new tests exercise the guard with the real notification store: queued old-session events cannot repopulate cleared state; disposed effects ignore later callbacks; a delayed foreground result cannot replace a new session's unread count. These are callback/store contract tests, not actual native socket-delivery evidence. All 49 mobile checks pass (`/tmp/rentos-mobile-notifications-final.log`), including the three new callback regressions and existing component flows. Mobile typecheck/lint and diff checks pass (`/tmp/rentos-mobile-notifications-types.log`, `/tmp/rentos-mobile-notifications-lint.log`).
- Remaining: native socket/reconnection/foreground evidence, push-notification response handlers, unread-count event semantics, same-session profile metadata ordering and wider provider/Ghana/store requirements. No real financial actions occurred.


### Push enrollment during permission and session changes — 13 September 2026

- Push registration captures the originating mobile session and checks it after channel setup, permission reads/prompts and project/token acquisition, and before submitting registration. Effect cancellation also prevents submission. OS/provider/network failures are contained; unsuccessful server registration no longer returns a token as though enrollment succeeded.
- Hook cleanup passes its originating session version to unregister, preventing the old effect from sending an unregister request under a replacement account. Notification-tap listeners now attach only for authenticated users, are recreated for fresh login generations and reject queued callbacks after cleanup/session change; existing route validation remains in place.
- Four portable enrollment regressions cover logout/effect cancellation during token acquisition, OS/server errors and acknowledged-current enrollment. These simulate the OS token provider; they do not establish real iOS/Android permission or delivery behavior. All 53 mobile checks pass (`/tmp/rentos-push-session-final.log`), including the four enrollment regressions and existing component flows. Mobile typecheck/lint and diff checks pass (`/tmp/rentos-push-session-types.log`, `/tmp/rentos-push-session-lint.log`).
- Remaining: server-owned device-token cleanup at logout/session revocation, requests already submitted before account replacement and their ordering, failed cleanup retries, notification recipient binding and native permission/delivery/reconnect evidence. Skipping stale cleanup avoids acting as the new account but does not revoke an old server registration. Broader Ghana/store gates remain open; no real push messages or financial actions were sent.


### Push-token input and query-boundary validation — 13 September 2026

- Push registration/unregistration previously checked only token truthiness and passed request values into MongoDB token filters. Added shared schemas requiring bounded nonempty printable scalar strings, supported expo/fcm/apns platform values and strict request fields. Query documents, arrays, control characters, whitespace and oversized input now receive 400 before service access; caller-supplied ownership fields are rejected.
- Service entry points repeat token/platform validation before constructing database queries, protecting non-route callers too. Ownership continues to come from the authenticated user and unregister remains owner-scoped. This closes the unsafe input path; no claim is made that a cross-account takeover was demonstrated against Mongoose update casting.
- Evidence: 24 new HTTP/service regressions pass (`/tmp/rentos-push-input-tests.log`). The standard run passed 952 tests and skipped 107 opt-in cases. With the dedicated MongoDB test URI explicitly enabled, all 1,059 tests across 106 files pass (`/tmp/rentos-push-input-full-mongo.log`). API typecheck/lint and diff checks pass (`/tmp/rentos-push-input-types.log`, `/tmp/rentos-push-input-lint.log`). No real push provider messages were sent.
- Further finding: web usePushNotifications submits platform web with only an endpoint, while DeviceToken and the push service support expo/fcm/apns. The unsupported web flow was already inconsistent and needs proper subscription/transport implementation; validation now rejects it explicitly. Session-owned device registration/revocation, old registration races, native delivery and broader Ghana/store gates remain open.

## Push registration cleanup on shared revocation — 13 September 2026

- `authService.revokeAllSessions` now deletes the affected user’s existing device-token registrations alongside refresh/biometric revocation. Logout-all, password change/reset and refresh replay use this shared path. Cleanup failures propagate rather than reporting successful completion.
- Three real-Mongo checks cover logout-all removal across platforms, another account retained, repeated logout-all, replay-triggered cleanup, and ordinary logout preserving registrations because device/session binding is not yet available. Password-change/reset routing was inspected; these new tests exercise logout-all and replay directly.
- Verification: all **1,062 API tests / 107 files pass**, including opt-in Mongo integrations (`/tmp/rentos-push-revocation-full.log`); API typecheck and lint pass (`/tmp/rentos-push-revocation-types.log`, `/tmp/rentos-push-revocation-lint.log`). A TypeScript union-model cleanup error in the new fixture was corrected with explicit model calls.
- Remaining: this removes registrations present when cleanup runs, not push already submitted to a provider. Authentication currently checks JWT purpose/expiry and account eligibility but does not enforce account-wide access-token revocation. A still-valid access token or in-flight registration can re-enroll; per-session device binding, server registration ordering, delivery races and actual native/provider verification remain open. This checkpoint does not claim immediate logout revocation of access JWTs or comprehensive push isolation.

## Account-wide access-token revocation — 13 September 2026

- Added a persisted account `sessionVersion` (legacy/default zero). Normal registration/password login/MFA completion/refresh and biometric access-token issuance include it. Required and optional HTTP authentication and new socket handshakes require a matching account version; absent legacy claims work only while the account remains at zero. Malformed versions fail closed.
- The shared revoker increments the account version before refresh/biometric/push cleanup and invokes the existing socket disconnect helper. Already issued old access JWTs now fail subsequent HTTP authentication, including push registration. The suspended-account fallback also checks the version. Counter comparison avoids timestamp/second-boundary ambiguity.
- Extended the real-Mongo push-revocation fixture to actual accounts. New assertions verify pre-revocation legacy acceptance, persisted increment, old/legacy-token rejection, optional-auth anonymity, current-version acceptance and old-token rejection after suspension. Full API suite: **1,063 tests / 107 files pass** (`/tmp/rentos-session-version-full.log`). Socket code wiring was inspected; no new live socket handshake/disconnect integration test was added.
- Remaining: requests already authenticated can still finish; an in-flight refresh/biometric exchange or MFA challenge can cross revocation and needs credential-generation binding. A socket handshake crossing disconnect also needs review. Download/reset/share capabilities are separate purposes and are not covered by the session-token counter in this checkpoint. Multi-instance socket delivery and real native push verification remain outstanding. This is not evidence that all session races or compliance requirements are complete.

- Final API typecheck and lint pass (`/tmp/rentos-session-version-types.log`, `/tmp/rentos-session-version-lint.log`). Typecheck first caught an unnecessary session-purpose comparison in the download-only path; that comparison was removed, retaining its existing download behavior.

## MFA challenge generation binding — 13 September 2026

- Confirmed that a pre-revocation MFA challenge could complete against a later account version and receive a current access token. Password login now signs the authenticated account version into its MFA challenge. Completion validates a nonnegative safe-integer version and requires it to equal the loaded account version before TOTP verification or token creation. Missing legacy claims remain valid only for version-zero accounts.
- Added eleven service regressions using real JWT signing/verification with mocked password/TOTP checks and repository/token storage. Nine tests failed before the fix; all eleven pass afterward. They cover old/future/malformed claims, current-version access issuance, legacy zero compatibility and login challenge binding. They do not constitute a new TOTP algorithm or real-database concurrency test.
- Verification: all **1,074 API tests / 108 files pass**, including enabled Mongo suites (`/tmp/rentos-mfa-version-full.log`); API typecheck/lint pass (`/tmp/rentos-mfa-version-types.log`, `/tmp/rentos-mfa-version-lint.log`). Baseline/focused logs: `/tmp/rentos-mfa-version-before.log`, `/tmp/rentos-mfa-version-after.log`.
- Remaining: revocation after the user snapshot is loaded but before refresh credential persistence can still overlap issuance; refresh and biometric records need generation binding. Single-use MFA challenge/replay behavior and distributed socket/registration races remain separate unfinished work. Overall compliance goal remains IN PROGRESS.

## Refresh credential generation binding — 13 September 2026

- `RefreshToken` now persists immutable `sessionVersion`, defaulting legacy credentials to zero. Every ordinary refresh issuance path passes the account snapshot’s version: registration, password login, MFA completion and refresh rotation. Exchange requires the record version to match the loaded account version before issuing successors.
- Three new real-Mongo regressions verify: a stale credential inserted after logout-all cleanup is rejected; a refresh held after reading its user snapshot and resumed after logout-all emits only old-generation credentials whose successor exchange is rejected; current nonzero-generation credentials can rotate repeatedly and retain their version. Earlier access-token tests verify old-generation JWT rejection.
- Verification: focused seven push/session integration checks and all **1,077 API tests / 108 files pass** (`/tmp/rentos-refresh-version-focused.log`, `/tmp/rentos-refresh-version-full.log`); API typecheck/lint pass (`/tmp/rentos-refresh-version-types.log`, `/tmp/rentos-refresh-version-lint.log`).
- Remaining: an overlapping issuance can still return success with already-invalid credentials; this checkpoint prevents those credentials from restoring the session rather than promising cancellation of every in-flight response. Biometric records still need equivalent generation binding. Per-device revocation, refresh replay policy/response-loss recovery, already-authenticated operations, socket handshake ordering, provider push races and separate download/share purposes remain open. Overall compliance goal remains IN PROGRESS.

## Biometric account-generation binding — 13 September 2026

- Biometric token records now carry immutable `sessionVersion`, default zero for legacy compatibility. Enrollment checks that the authenticating token and loaded account share a version before password verification/replacement, then saves the authenticated version. This prevents a request authenticated before account-wide revocation from adopting a subsequently loaded account generation.
- Exchange rejects a record whose version differs from the account; rotated biometric credentials retain the record version. Access tokens continue using the matching account snapshot version. A later account-wide increment therefore invalidates both credentials, including credentials persisted after cleanup from an older snapshot.
- Seven new HTTP route regressions (mocked models, authentication middleware and password verifier) cover old/missing generations against a revoked account, current/legacy-zero exchanges, stale-auth enrollment rejection and ignoring a caller-supplied version. These verify route behavior, not hardware biometrics or database race ordering.
- Verification: **1,084 API tests / 109 files pass**, including enabled Mongo suites (`/tmp/rentos-biometric-version-full.log`); focused seven route tests pass (`/tmp/rentos-biometric-version-focused.log`); API typecheck/lint pass (`/tmp/rentos-biometric-version-types.log`, `/tmp/rentos-biometric-version-lint.log`).
- Remaining: biometric-only revoke-all/replay and per-device revocation currently update records without a generation barrier and need an in-flight exchange audit. Actual native enrollment/exchange, distributed socket/push races and broader compliance release evidence remain unfinished. Overall goal stays IN PROGRESS.

## Biometric-only revocation generation — 13 September 2026

- Added an independent account `biometricVersion` and immutable biometric-record version. Biometric revoke-all and replay handling increment it before updating token records. Enrollment saves the account snapshot version, exchange requires a matching version and successors preserve it. Enrollment authenticated with a biometric-issued access token also checks that token’s biometric version against the loaded account.
- New biometric-issued access JWTs include the biometric version; required/optional HTTP authentication and new socket handshakes check it. Ordinary login JWTs omit it and remain governed by the existing account session version. Zero-version database matching is combined with the account session filter without overwriting its legacy `$or` condition.
- Four new HTTP route tests verify revoke-all/replay update ordering, rejection despite a matching account session version and nonzero-generation propagation. One real-Mongo middleware test verifies biometric access rejection after increment, continued ordinary-token acceptance and current biometric-token acceptance. Focused 19 tests pass (`/tmp/rentos-biometric-revoke-focused.log`); full **1,089 API tests / 109 files pass** (`/tmp/rentos-biometric-revoke-full.log`); API typecheck/lint pass (`/tmp/rentos-biometric-revoke-types.log`, `/tmp/rentos-biometric-revoke-lint.log`).
- Remaining: already-authenticated requests and existing socket connections are not canceled by this biometric-only counter. Older biometric access JWTs without the distinguishing claim are indistinguishable from ordinary JWTs and remain subject to expiry/account-wide revocation. Per-device revocation versus rotated successors, replay scope/response-loss policy, native-device behavior and distributed socket/push evidence remain open. Full compliance goal stays IN PROGRESS.

## Live biometric socket disconnection — 13 September 2026

- Authenticated sockets with a biometric-version claim join an account-specific biometric room. Biometric revoke-all/replay invoke a targeted disconnect helper immediately after incrementing the database generation, before token-record cleanup. Ordinary login connections are outside that room. Route regressions assert both paths invoke the helper for the correct account.
- Added a real Socket.IO client/server integration using isolated Mongo account fixtures. It verifies targeted biometric disconnect, continued event delivery to ordinary and other-account sockets, rejected old biometric reconnect, current biometric connection, subsequent account-wide disconnect and old ordinary reconnect rejection. Conversation/contact discovery is mocked to avoid unrelated presence fixtures. Added Socket.IO client 4.8.3 as an API dev dependency using the local package cache for this test.
- Verification: focused live integration passes (`/tmp/rentos-socket-revoke-focused.log`); all **1,090 API tests / 110 files pass** with Mongo enabled (`/tmp/rentos-socket-revoke-full.log`); API typecheck/lint pass (`/tmp/rentos-socket-revoke-types.log`, `/tmp/rentos-socket-revoke-lint.log`).
- Remaining: a connection whose authentication check overlaps generation increment/room admission still needs an admission-order fix. Legacy biometric sockets without the claim cannot be distinguished from ordinary login sockets. No distributed adapter or production/native-device delivery was exercised. Per-device token rotation/revocation, in-flight operations and broader compliance gates remain open. Overall goal remains IN PROGRESS.

## Socket admission and revocation ordering — 13 September 2026

- Socket connections now enter revocation-only account/biometric rooms before a second account eligibility/generation lookup. Private account-room admission, online presence and application handlers follow only if the lookup succeeds and the connection is still active. Account-wide disconnection targets the revocation room, covering connections awaiting their second check. Lookup failures disconnect.
- Early client packets wait on an admission promise, so adding the asynchronous check does not drop messages emitted immediately after the client’s connect event. No private account data is broadcast to the revocation-only rooms.
- Added four real Socket.IO/Mongo race cases: account-wide and biometric-only revocation while the first auth result is held, and each while the second result is held. They verify disconnection and absence from private rooms. An additional early `get:online` check verifies buffered request delivery. The existing live disconnect/reconnect/account-isolation test also passes.
- Verification: all **1,095 API tests / 110 files pass** with opt-in Mongo enabled (`/tmp/rentos-socket-admission-full.log`); API typecheck/lint pass (`/tmp/rentos-socket-admission-types.log`, `/tmp/rentos-socket-admission-lint.log`). Earlier focused run covered four tests before expanding to both check stages (`/tmp/rentos-socket-admission-focused.log`).
- Remaining: this exercises the current single-process adapter. Distributed adapter behavior, long-lived JWT expiry, asynchronous presence/authorization callbacks, legacy biometric sockets and native/production delivery remain open. Full compliance goal remains IN PROGRESS.

## Socket token lifetime enforcement — 13 September 2026

- Socket JWTs must include a numeric expiry that converts safely to milliseconds. Connections schedule disconnection at the absolute deadline, recheck expiry at admission and incoming packets, and cancel their timer on disconnect. Long deadlines use bounded timer intervals with deadline rechecks, avoiding the platform timer-overflow behavior.
- Real Socket.IO/Mongo tests verify an idle two-second token is disconnected and removed from its private room, a freshly signed token reconnects and serves a request, and a signed token without expiry is rejected. Two timer tests cover exact deadline, cancellation and a deadline beyond the timer’s maximum interval.
- Verification: focused ten tests pass (`/tmp/rentos-socket-expiry-focused.log`); all **1,099 API tests / 111 files pass** with Mongo enabled (`/tmp/rentos-socket-expiry-full.log`); API typecheck/lint pass (`/tmp/rentos-socket-expiry-types.log`, `/tmp/rentos-socket-expiry-lint.log`).
- Remaining: the reconnect test supplies a fresh token explicitly; automatic web/mobile recovery after server-initiated expiry still needs inspection and verification. Distributed clocks/adapters, blocked-event-loop timing, pending asynchronous callbacks, per-device revocation and broader compliance release gates remain open. Goal stays IN PROGRESS.

## Client expiry recovery wiring — 13 September 2026

- Server token-expiry disconnection now emits `session:expired` first. The real socket expiry integration asserts that this signal is delivered before observing completion. Other revocation disconnects do not emit that event.
- Web and mobile root socket hooks listen for expiry and revalidate `/users/me` through their existing authenticated HTTP client, reusing each client’s refresh/session handling. Shared recovery logic coalesces repeated signals and guards the originating login/effect before and after the request. Reconnect callbacks require changed credentials and the original socket still being current; token updates also rerun the existing hook lifecycle. Cleanup disposes the recovery and unregisters its listener.
- Four shared contract checks pass for duplicate signals, logout, disposal and failed revalidation (`/tmp/rentos-socket-recovery-checks.log`). These execute under Playwright but do not launch a browser or verify mounted React hooks. All **1,099 API tests / 111 files pass** with Mongo enabled (`/tmp/rentos-socket-recovery-api-full.log`). API/web/mobile typecheck and lint pass (`/tmp/rentos-socket-recovery-{api,web,mobile}-{types,lint}.log`).
- Remaining: actual hook-driven web/native automatic reconnect needs an end-to-end walkthrough. Revalidation/network failure has no new retry schedule; initially expired tokens rejected during handshake do not receive the expiry signal. Mobile HTTP refresh outage semantics, distributed delivery and broader compliance gates remain open. Overall goal remains IN PROGRESS.

## Mounted web socket recovery verification — 13 September 2026

- Added three real Chromium tests against the mounted web application. Tests use the existing authenticated fixture, then control HTTP refresh responses and WebSocket frames. The actual Socket.IO browser client, React socket hook, auth store/refresh coordinator and notification renderer execute. A pre-expiry notification verifies installed listeners before triggering the recovery scenario.
- Successful recovery receives an expiry signal/server disconnect, encounters an expired-token HTTP 401, makes exactly one refresh call, reconnects with the rotated token and displays a post-reconnect notification. Held-refresh cases separately log out or perform a fresh same-account login; the old refresh response must not reconnect with its returned credentials. Tests await the refresh response before checking preserved auth state and stale reconnect absence.
- All **three browser checks pass** (`/tmp/rentos-socket-recovery-browser-final.log`, artifacts `/tmp/rentos-socket-recovery-browser-final`). The initial two-case run also passed (`/tmp/rentos-socket-recovery-browser.log`). No production code changed in this checkpoint; the last full API/typecheck/lint evidence remains in the preceding checkpoint.
- Test frame encoding was checked against the official [Socket.IO protocol specification](https://github.com/socketio/socket.io-protocol). This controlled transport test complements the previous real Socket.IO/Mongo server tests; it does not prove one unmocked expiry-to-refresh-to-reconnect roundtrip. Actual native recovery, initial-handshake expiry, transient recovery failure and broad compliance/release gates remain open. Overall goal stays IN PROGRESS.

## Mobile refresh outage handling — 13 September 2026

- Confirmed both ordinary and biometric mobile refresh paths returned false for every caught error/non-OK response, causing `createSessionRequests` to log out valid accounts during outages. Both now use a shared bounded response validator: only explicit 401/403 rejection returns null; network/rate-limit/server/malformed-response failures throw without triggering logout. Successful responses require nonempty string access and rotated-refresh tokens.
- A ten-second AbortController deadline bounds the fetch/body work and clears its timer on completion. Existing originating-session checks and SecureStore update paths remain in place. Device-id and biometric persistence failures now propagate instead of being treated as invalid credentials.
- Ten new portable tests combine the real response helper/session-request coordinator to verify network, 503, 429, invalid JSON, missing rotation, wrong token type, 401/403 rejection, successful validation and an actually timed ten-second abort. Added them to the mobile test configuration. All **63 mobile checks pass** (`/tmp/rentos-mobile-refresh-suite.log`, artifacts `/tmp/rentos-mobile-refresh-suite`), including Expo-web screen flows and portable contracts. Focused ten checks pass (`/tmp/rentos-mobile-refresh-final.log`); mobile typecheck/lint pass (`/tmp/rentos-mobile-refresh-types.log`, `/tmp/rentos-mobile-refresh-lint.log`).
- Remaining: this does not establish native-device offline/foreground behavior. A provider may rotate a credential before its response is lost; preserving the login does not recover that lost successor. SecureStore failure after rotation, initially expired socket handshakes, failed socket-recovery retry and broader compliance gates remain open. Overall goal stays IN PROGRESS.

## Mobile delayed 401 rotation reuse — 13 September 2026

- Reproduced a second refresh when one request’s old-token 401 arrived after another request had fully rotated credentials. The mobile request coordinator now compares the current access token after validating the originating login generation; it reuses a newer nonempty token rather than refreshing again. Login/account-change guards remain in place before retry and response delivery.
- Added a deterministic delayed-response test (failed against the original implementation) and a test verifying a transient rejected refresh promise is removed so a later request can retry successfully without logout. Existing concurrent-refresh and stale-session tests remain green.
- Verification: **21 focused checks pass** across mobile session requests, refresh response/timeout handling and shared socket recovery (`/tmp/rentos-mobile-late-401-after.log`). Baseline: one failing new late-response regression, six passing (`/tmp/rentos-mobile-late-401-before.log`). Mobile typecheck/lint pass (`/tmp/rentos-mobile-late-401-types.log`, `/tmp/rentos-mobile-late-401-lint.log`). These are portable contract checks; the preceding 63-check full mobile run predates this coordinator change.
- Remaining: actual mobile socket recovery, initially expired handshakes, response-loss recovery, biometric per-device revocation and wider compliance/release gates remain open. Overall goal stays IN PROGRESS.

## Mobile socket instance continuity and mounted recovery — 13 September 2026

- Found that `connectSocket` recreated a socket whenever it was not yet connected, removing all existing listeners. Calls from multiple screens during initial connection could therefore destroy other consumers’ listeners. Credential rotation also recreated the object. Extracted a shared connection manager that reuses pending/disconnected instances and reconnects the same object with updated credentials; explicit logout still removes listeners and destroys the instance. Socket.IO continues to own transient transport reconnection.
- Three portable connection-manager checks verify pending-instance sharing, rotation preserving object/listeners and logout cleanup/new-instance creation. Added a real Chromium test of the mounted mobile app through Expo web, controlling HTTP/WebSocket responses: authenticate, display an initial notification, deliver expiry/disconnect, make exactly one refresh, reconnect with rotated credentials and display a new notification. This executes the actual mobile hooks, store, socket client and notification UI.
- Verification: **69 mobile checks pass** (`/tmp/rentos-mobile-socket-suite.log`, artifacts `/tmp/rentos-mobile-socket-suite`), covering this change and the preceding delayed-401 fix. Focused mounted recovery passes (`/tmp/rentos-mobile-socket-mounted.log`); seven connection/recovery contracts pass (`/tmp/rentos-mobile-socket-identity.log`). Mobile typecheck/lint pass (`/tmp/rentos-mobile-socket-types.log`, `/tmp/rentos-mobile-socket-lint.log`).
- Remaining: Expo-web execution is not authenticated native-device evidence. Initial handshake rejection for expired tokens, failed recovery retry, chat-room rejoin across transport reconnect, per-device revocation and wider compliance/release gates remain open. Overall goal stays IN PROGRESS.

## Conversation room reconnection and E2E socket isolation — 13 September 2026

- Web/mobile conversation screens previously joined only when their effects ran. They now use a shared room subscription that joins on each connection and immediately if already connected. Cleanup removes the reconnect listener and leaves only while connected, preventing stale queued join/leave commands from abandoned screens. Added room/recovery contracts to the mobile suite configuration.
- Seven focused room/recovery contracts pass (`/tmp/rentos-chat-rejoin-checks.log`); the same command also matched and skipped the Expo-only mounted test because MOBILE_WEB_URL was unset. Web/mobile typecheck and lint pass (`/tmp/rentos-chat-rejoin-{web,mobile}-{typecheck,lint}.log`).
- The real browser chat-safety regression initially failed to display a post-unblock message. Diagnostic socket frames showed an invalid-token handshake, not an admitted room failure. The checked-in web `.env` set VITE_SOCKET_URL to development port 3002, while `e2e:dev` overrode HTTP to the isolated API but omitted the socket override. Added an explicit VITE_SOCKET_URL override to the root E2E launcher. Removed temporary diagnostic instrumentation after tracing the cause.
- Started an isolated Vite instance on 5475 with HTTP/socket proxy targeting test API 3402. The real browser flow now passes: bilateral block enforcement, unblock, live incoming message delivery, reporting/ownership and reciprocal blocking (`/tmp/rentos-chat-rejoin-browser-final.log`, artifacts `/tmp/rentos-chat-rejoin-browser-final`). This verifies the existing chat-safety regression with corrected runtime configuration, not a forced-disconnect browser scenario.
- Remaining: mounted-chat forced transport reconnection, missed-message/history catch-up and native device behavior still need verification. Broader compliance/release gates remain open. Overall goal remains IN PROGRESS.

## Real open-chat transport interruption — 13 September 2026

- Added a real Chromium/Socket.IO/API test with isolated disposable accounts and a peer socket. Playwright forwards socket frames to the actual test server, records conversation join requests and closes the browser transport once. After reconnect, a typing event observed by the peer proves usable room membership, then an API-created message must appear without reloading the open thread. Fixture accounts and the peer connection are cleaned up.
- Baseline failed after reconnect: the browser input contained the post-interruption draft but no new typing event reached the peer. The existing outgoing typing flag remained true until its debounce timer, suppressing the first new event. Both web/mobile conversation screens now reset outgoing typing state/timers and received typing indicators on connect/disconnect, unregistering those callbacks on cleanup.
- The final unmocked socket/API reconnect test passes on isolated Vite 5475/API 3402 (`/tmp/rentos-chat-transport-final.log`, artifacts `/tmp/rentos-chat-transport-final`). Baseline evidence: `/tmp/rentos-chat-transport.log`. Web/mobile typecheck and lint pass (`/tmp/rentos-chat-typing-{web,mobile}-{typecheck,lint}.log`).
- Remaining: the real interruption walkthrough covers the web chat; mobile logic is implemented and type/lint checked but has not undergone this exact forced-transport/device scenario. Messages missed while offline, read-state synchronization, initial expired handshakes and wider compliance gates remain open. Overall goal stays IN PROGRESS.

## Web missed-message catch-up after reconnect — 13 September 2026

- Expanded the real browser/Socket.IO/API reconnect test to hold the new transport handshake while a peer sends a message. Baseline restored typing and new live messages but failed to show the message sent during the interruption (`/tmp/rentos-chat-catchup-before.log`).
- Web chat now invalidates/refetches the active conversation’s exact message query plus conversation-list and unread-count queries on socket connect. The handler is removed on cleanup and follows the active conversation. Existing room-rejoin and typing reset behavior remains.
- Final live test passes: the offline message and a newly sent post-reconnect message both appear without a page reload, and peer typing still works (`/tmp/rentos-chat-catchup-final.log`, artifacts `/tmp/rentos-chat-catchup-final`). Web typecheck/lint pass (`/tmp/rentos-chat-catchup-typecheck.log`, `/tmp/rentos-chat-catchup-lint.log`). No API implementation changes were needed.
- Remaining: this refreshes the existing messages query/page; it does not prove catch-up across an arbitrarily long/multi-page gap. Snapshot responses racing live cache updates, mobile catch-up, read-state convergence and broader compliance/release gates remain open. Overall goal remains IN PROGRESS.

## Mobile message-history recovery — 13 September 2026

- Added an Expo-web browser test that logs in, opens Messages and a conversation, confirms initial history, interrupts the socket transport and verifies room re-entry plus recovery of a message now present in HTTP history. The initial test mistakenly used a full navigation that discarded the fixture’s in-memory login; after switching to in-app navigation, baseline reproduced the missing message despite successful reconnect/rejoin (`/tmp/rentos-mobile-chat-baseline2.log`). An earlier launch also correctly stopped on an already-used default web-server port; the final runs use the mobile configuration with no web-server startup.
- Mobile chat now reloads its messages and conversation details and requests online presence on connect. Cleanup removes the catch-up callback alongside room and typing listeners. Existing request/session guards continue to apply.
- Five focused checks pass: mounted mobile missed-history recovery, mounted expiry/refresh/notification recovery and three room lifecycle contracts (`/tmp/rentos-mobile-chat-final.log`, artifacts `/tmp/rentos-mobile-chat-final`). Mobile typecheck/lint pass (`/tmp/rentos-mobile-chat-typecheck.log`, `/tmp/rentos-mobile-chat-lint.log`). The new test is included in the mobile suite configuration.
- Remaining: HTTP/socket responses are controlled in the Expo-web test, not native-device or unmocked provider evidence. Only the existing history page is reloaded; multi-page gaps, live messages arriving during a snapshot load, read-state convergence and broader compliance gates remain open. Overall goal remains IN PROGRESS.

## Mobile history snapshot versus live messages — 13 September 2026

- Expanded the mounted Expo-web catch-up test to hold the HTTP history response, deliver a live socket message, confirm it appears, then release older history. Baseline proved the live message disappeared afterward (`/tmp/rentos-mobile-snapshot-before.log`).
- Added a per-request message snapshot buffer. Live updates/removals received during the active request are applied over the returned snapshot, keyed by message id; older/superseded or canceled snapshots cannot commit. Conversation ids scope updates. Mobile chat records incoming events and confirmed send results in the active buffer. A separate pending-send map overlays optimistic messages while awaiting confirmation; success/failure removes the pending entry and confirmation avoids duplicate ids. Added an accessible Send message button label.
- Final mounted test verifies missed history and a live message both survive, a pending outgoing bubble remains through snapshot completion, and confirmation leaves exactly one bubble. Two portable contracts cover update precedence/deduplication/removal and superseded/canceled/cross-conversation results. All three pass (`/tmp/rentos-mobile-snapshot-send.log`, artifacts `/tmp/rentos-mobile-snapshot-send`); mobile typecheck/lint pass (`/tmp/rentos-mobile-snapshot-typecheck.log`, `/tmp/rentos-mobile-snapshot-lint.log`). The helper tests are in the mobile suite configuration.
- Remaining: these tests use controlled Expo-web transport/HTTP responses. Web query-cache races, long/multi-page history gaps, wider screen-navigation callback ordering and native-device behavior remain open. Overall compliance goal remains IN PROGRESS.


### Incremental publication — 13 September 2026

User authorized pushing verified fixes as batches become ready. `2d74e3c` publishes mobile socket reuse and listener preservation with three passing focused checks. `3b449f1` publishes AI request deadlines, response limits, fixed provider origins, sensitive-error/log sanitization and original-index batch embedding alignment. The AI batch was tested independently in a separate checkout containing HEAD plus only the six staged files, after the normal shared-types prebuild: 569 API tests across 57 files, typecheck and lint pass. Both pushes to `origin/main` succeeded and remote heads were verified. Remaining local engineering changes and external compliance obligations are not represented as published or complete.


### Backend publication — 13 September 2026

Published `0acf455` to `origin/main`; remote head verified. The API/shared-contract batch passed independently of uncommitted clients: 1,099 tests across 111 files with the isolated MongoDB suite enabled, plus API typecheck and lint. See `docs/compliance/backend-release.md` for the release scope and limits. Web/mobile publication, fresh dependency installation, production migration/provider verification, native-device evidence and external legal/store obligations remain outstanding. This records engineering publication, not production deployment or overall compliance completion.


### Web publication — 13 September 2026

Web publication checkpoint (13 September): published `9efdfdd` (`feat(web): connect compliance controls and isolate user sessions`) to origin/main. Independent staged-checkout production build and lint pass. Initial browser run passed 23/24 and exposed deletion confirmation lost on the session-provider remount. Fixed routing/history-state confirmation; final five privacy checks pass, including deletion from public and Settings entry points and old-token rejection. Scope and verification limits are in docs/compliance/web-release.md. Mobile publication and remaining compliance gates stay open. Overall goal remains IN PROGRESS.


### Mobile publication — 13 September 2026

Mobile publication checkpoint (13 September): published `c305af8` (`feat(mobile): add compliance flows and resilient session recovery`) to origin/main. All 79 existing mobile checks pass. A new mounted regression reproduced deletion confirmation disappearing on logout; the persistent root notification fix passes its separate check, including visibility beyond the ordinary toast timeout. Independent-checkout typecheck/lint pass. After a clean cached lockfile install without lifecycle scripts, Android/iOS/web exports pass. The failed reused-symlink export and verification limits are recorded in docs/compliance/mobile-release.md. Native-device/provider/store approval and broader compliance gaps remain open. Overall goal remains IN PROGRESS.


### Apple purchase data export — 13 September 2026

Apple purchase export checkpoint (13 September): reproduced omission of the Apple purchase journal from account export. Added ownership-scoped applePurchases with an explicit product/date/renewal/access allowlist, excluding encrypted identifiers and recovery internals; updated API documentation. New real-Mongo HTTP tests verify inclusion, query-parameter impersonation resistance, private-field exclusion and authentication. All 1,101 API tests across 112 files pass, plus typecheck/lint. This closes the Apple journal omission; other collection coverage, retention classification and wider compliance gates remain open. Overall goal remains IN PROGRESS.

Evidence: the baseline Apple export test failed because `data.applePurchases` was absent. The final suite passes with opt-in isolated MongoDB enabled (`/tmp/rentos-apple-export-tests.log`); typecheck and lint pass. The HTTP test uses real journal records and queries while mocking only account admission, and cleans up its own fixture rows. This is API export evidence, not a native share-sheet or complete data-inventory audit.


### Google purchase export fields — 13 September 2026

Google purchase export checkpoint (13 September): reproduced recovery leases/errors, token hashes, prepared grants and an unexpected nested provider field being included in personal export. Google journal queries now use an explicit top-level and nested-item allowlist while retaining owned product/order/renewal/access data. Two real-Mongo HTTP checks verify ownership, internal-field exclusion and authentication; all 1,103 API tests across 113 files plus typecheck/lint pass. Remaining export inventory and broader compliance requirements stay open. Overall goal remains IN PROGRESS.

Baseline evidence: `/tmp/rentos-google-export-before.log` shows internal-field fixture markers in the authenticated export, including an extra nested field inserted directly into MongoDB. The final suite (`/tmp/rentos-google-export-tests.log`) includes the opt-in isolated MongoDB tests. Authentication admission is mocked only for the owned-record test; journal storage/query/projection and HTTP serialization are real. Fixtures are scoped and removed after testing. This does not establish complete personal-data export coverage.


### Complete retained audit history export — 13 September 2026

Audit history export checkpoint (13 September): reproduced silently truncated personal audit history at 1,000 records. Removed the cap, added deterministic newest-first ordering and declared an account/history index. A real-Mongo HTTP regression verifies all 1,005 owned fixture records, oldest/newest inclusion, no duplicates and other-account exclusion despite an impersonation query parameter. All 1,104 API tests across 114 files pass with four workers, plus typecheck/lint. Default-concurrency verification hit one unrelated store-lifecycle timeout; the bounded rerun passes without changed assertions or timeouts. Production index rollout, large-export resource limits and full data inventory remain open. Overall goal remains IN PROGRESS.

The endpoint no longer returns only the latest 1,000 audit records. This aligns it with the other currently unpaginated export groups; it does not establish scalability for arbitrarily large accounts. Evidence: baseline `/tmp/rentos-audit-export-before.log`, final opt-in MongoDB suite `/tmp/rentos-audit-export-tests-final.log`. Account admission is mocked for this regression; persistence, projection, ordering and HTTP response are real. Fixture rows are cleaned up.


### Borrower financing personal-data export — 13 September 2026

Borrower financing export checkpoint (13 September): reproduced missing financing applications, contracts, loans and credit-score history. Added these groups scoped to authenticated applicant/user identity; financier/admin roles and query parameters do not expand the personal export to other borrowers. Two real-Mongo HTTP checks verify income currency, repayment schedules, loan amounts, score history and cross-account exclusion. All 1,106 API tests across 115 files pass with two workers, plus typecheck/lint. The long store-lifecycle integration scenario now has an explicit 20-second budget after repeated default five-second timeouts, with assertions unchanged. Broader data inventory, business/financier data-rights classification and release gates remain open. Overall goal remains IN PROGRESS.

The baseline failed both new export cases because the groups were absent. Verification uses isolated MongoDB persistence and HTTP serialization, with account admission mocked. Fixtures are inserted directly for export testing and cleaned up by fixture ownership. The tests do not exercise credit decisions, disbursement or legal classification of financial activities. Evidence: `/tmp/rentos-financing-export-before.log` and `/tmp/rentos-financing-export-tests-final.log`.


### Investment and insurance personal-data export — 13 September 2026

Investment and insurance export checkpoint (13 September): reproduced missing investment holdings and insurance policies/claims. Added both groups scoped to authenticated userId, retaining maturity/return amounts, premium details and embedded claim outcomes. Real-Mongo HTTP tests verify ownership despite query impersonation, partner/property linkage and privileged claim-review roles. All 1,108 API tests across 116 files pass with two workers; the final two cases also pass after using actual admin/super_admin claim-review permissions. Typecheck/lint pass. Broader collection coverage, retention classification, regulated-service evidence and release gates remain open. Overall goal remains IN PROGRESS.

Baseline: both new cases failed because the export groups were absent (`/tmp/rentos-investment-export-before.log`). Final suite: `/tmp/rentos-investment-export-tests.log`; strengthened role/permission cases: `/tmp/rentos-investment-export-final.log`. Fixtures use validated model creation, real query/serialization and scoped cleanup; account admission is mocked. No investment placement, policy purchase, claim payout or provider operation is performed by these tests. This verifies export engineering, not regulatory authorisation of investment or insurance services.


### Account activity and saved preferences export — 13 September 2026

Account activity export checkpoint (13 September): verified existing saved settings export and reproduced omitted favorites, notifications, achievements and payment-streak history. Added all four groups scoped to authenticated userId. A real-account/Mongo/HTTP regression checks preferences, favorite identity, read notification content, award metadata, streak breaks, cross-account exclusion and password/MFA secret exclusion without mocking authentication. All 1,109 API tests across 117 files pass with two workers, plus typecheck/lint. Complete collection inventory, retention classification and wider compliance gates remain open. Overall goal remains IN PROGRESS.

The baseline failed on missing favorites (`/tmp/rentos-activity-export-before.log`). Final opt-in MongoDB suite: `/tmp/rentos-activity-export-tests.log`. The regression uses two unique persisted accounts, validates actual JWT/account admission and removes its own related rows afterwards. Existing user.settings fields were already exported and needed verification rather than duplicate implementation. This does not establish complete export coverage across remaining marketplace, employment, tenancy operations and other collections.


### Settings validation and concurrent preference updates — 13 September 2026

Settings API checkpoint (13 September): reproduced eight malformed-input acceptances and a lost notification choice when two updates overlap. Added strict theme/language/boolean notification validation, atomic dotted-field writes and consistent default merging on read/update. Ten real-account/Mongo/HTTP tests pass, including a barrier-controlled old save race; all 1,119 API tests across 118 files, typecheck and lint pass. Client follow-up is required: web/mobile currently submit whole notification snapshots, and mobile initializes preferences without reading saved settings or surfacing errors. Those client gaps and broader compliance gates remain open. Overall goal remains IN PROGRESS.

Baseline: 9/10 regressions failed (`/tmp/rentos-settings-before.log`). Final opt-in MongoDB suite: `/tmp/rentos-settings-tests.log`. The overlap test pauses the former document-save path until both requests have read their settings; the new atomic update path retains both independent changes. Unsupported keys, non-boolean channel values and unsupported appearance/language values return 400. This is server-side engineering evidence; it does not yet establish correct client hydration, failure feedback or enforcement of all notification delivery preferences.
